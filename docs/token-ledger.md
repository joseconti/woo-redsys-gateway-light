# Token Ledger — Payment Gateway for Redsys & WooCommerce Lite

> One row per working session. Final reconciliation (cost + deviation vs estimate) at release — no client budget, so this tracks the author's own usage for reference.

| Date | Session | Model | Notes |
|---|---|---|---|
| 2026-07-31 | Keel skill maintenance (v1.11.0 → v5.9.0 embed update) | Claude Sonnet 5 | Paused mid-task, continued 2026-08-01 |
| 2026-08-01 | Keel skill maintenance completion + session-setup batch (automatic mode, forge issues, notifications) | Claude Sonnet 5 | Pushed `develop`, wrote `.claude/settings.local.json` + `CLAUDE.local.md` |
| 2026-08-01 | Full Keel adoption (this session) | Claude Sonnet 5 | Inventory, 6 batched questions, license reconciliation (D-003), reference-material relocation (D-006), full `docs/` state + as-built artifacts + gap audit |
| 2026-08-01 | Phase 5 sprint: PHPUnit test suites (all 4 gateways) + Playwright checkout smoke test + full-plugin review (9 real bugs fixed, 1 deferred) | Claude Sonnet 5 | Unit tests for `RedsysLiteAPI` (D-016/D-017); integration tests for `WC_Gateway_redsys` (D-018), `WC_Gateway_Bizum_Redsys` (D-019, real WC_Order fixture), `WC_Gateway_GooglePay_Redirection_Redsys` (D-020), `WC_Gateway_Inespay_Redsys` (D-021, own signature algorithm + WPDieException); Playwright checkout smoke test (D-022); found + escalated + fixed a real signature-bypass vulnerability in GooglePay (D-020); full-plugin review via 4 parallel agents (every finding independently re-verified by reading the actual code) found 9 real bugs — fixed the wrong-secret bug in `successful_request()` for Bizum + GooglePay (D-023), then all 7 remaining findings at the user's request (D-026: a `sleep(5)` DoS, a pre-auth crash risk in two gateways, a ~1-in-5 wrong-order bug, non-constant-time comparisons, an Inespay refund/payment mixup, two Inespay amount bugs) — deferring only the plaintext-secret-in-order-meta finding by the user's explicit choice (D-025). 40 automated tests total, all but one (code-review-verified only) mutation/regression-verified against the pre-fix code. L-003 and L-004 recorded. 12 commits pushed to `develop`. Multiple security/correctness fixes unreleased — flagged as a release priority. |
| 2026-08-01 | Phase 5 sprint: closed all 4 remaining testability gaps (D-027–D-031) | Claude Sonnet 5 | Bizum + Google Pay checkout e2e tests (D-027, also fixed a real test-order dependency in the existing Redsys e2e test); WooCommerce Blocks checkout e2e test (D-028, first test of any kind for that pre-existing integration); Inespay checkout e2e test via a dev-only `pre_http_request` stub since `process_payment()` makes a real server-side API call (D-029); `disable_inespay()` fractional-total coverage (D-030, whose first draft couldn't have caught its own target bug until mutation-testing caught that — L-005 recorded, an opcache/wp-env quirk); recorded a decision against JS unit-test tooling (D-031). 46 automated tests total (8 unit + 31 integration + 7 e2e), all green, all new tests mutation-tested. 1 commit pushed to `develop` (7bcee84). Still unreleased: this sprint's coverage plus the prior session's 9 bug fixes — release remains a priority to raise next session. |
| 2026-08-02 | Phase 5 sprint: fixed the "1 billion bug" (D-033), triggered by a real customer support ticket | Claude Sonnet 5 | Verified a customer-reported HTTP 500 ("ID del pedido no válido") against current code; confirmed D-026's earlier fix is unrelated to a second, deeper bug — order IDs of 10+ digits lose high-order digits in `prepare_order_number()`/`clean_order_number()`, and `process_refund()` never renewed the order-number transient. Ported the fix from the sibling premium plugin (`woocommerce-gateway-redsys` v31.0.3): a persistent postmeta reverse-lookup fallback plus a 24h refund-time transient renewal, across `class-wc-gateway-redsys-global-lite.php` and the three affected gateways. 5 new regression tests, mutation-tested for real (reverted via `git checkout`/`git apply`, confirmed all 4 targeted assertions fail against pre-fix code, restored, confirmed 36/36 integration green). L-006 recorded (test-data-boundary lesson: D-026's own regression test never used an order ID large enough to reveal this). `readme.txt` updated. 51 tests total. 1 commit pushed to `develop` (fa8d88b). Still unreleased: this fix plus all prior session's fixes — release remains the standing next-action priority. |
