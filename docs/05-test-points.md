# Test Points — Payment Gateway for Redsys & WooCommerce Lite

> The scaffold's first entry below is a playground/environment verification
> run during adoption, not a sprint slice with acceptance criteria — no
> `AC-nn` exists yet since no new work has been scoped. Future rows follow
> the sprint-slice shape; do not invent a row for work that has not actually
> been driven and evidenced.

| Slice | Criterion (AC-nn) | Coverage (`driven` or a delegation tag) | Delegation steps (when tagged) | Real run in playground | Security checks | Accessibility (automated + AT pass) | i18n (strings externalized) | Reuse checked (no new duplicate) | Docs updated (api/reference, example runs) | Extension points exposed (filter/action) | Evidence (commands + output summary + commit) | Result | Notes |
|-------|--------------------|-------------------------------------------|----------------------------------|--------------------------|-------------------|----------------------------------------|--------------------------------|--------------------------------------|------------------------------------------------|------------------------------------------------|--------------------------------------------------|--------|-------|
| Adoption — playground verification | — | driven | — | yes — wp-env, WordPress 7.0 + WooCommerce 7.4.0 | fail-closed notification control driven-verified (2 fabricated POSTs to `?wc-api=WC_Gateway_redsys`, both rejected, order stayed `wc-pending`) | not run this pass (see `docs/accessibility.md`, TO BUILD) | n/a — no strings changed | n/a — no code changed | `docs/threat-model.md` and `docs/playground.md` updated with real evidence | n/a | `npx wp-env start`; `wp plugin activate woo-redsys-gateway-light`; `wp eval-file` to create order #13; two `curl -X POST` to the IPN endpoint; `wp post get 13 --field=post_status` → `wc-pending` both times | PASS | Plugin did not auto-activate on first `wp-env start` — see `docs/lessons-learned.md` L-002 |
| Issue #93 — replace deprecated `wc_enqueue_js()` with `wp_add_inline_script()` | — | driven | — | yes — wp-env, WordPress 7.0 + WooCommerce 7.4.0 | n/a | not applicable — no UI copy changed | n/a — no strings changed | reused the exact fix pattern the reporter proposed across all 4 call sites (no new helper introduced — see `docs/04-adoption-audit.md` Duplication note for a possible future DRY pass) | `docs/api/INDEX.md` unaffected (internal, not a public surface); `docs/issues.md` E-001 entry added | n/a | `php -l` on all 3 edited files (no syntax errors); `grep -rn wc_enqueue_js` → 0 remaining occurrences; direct `receipt_page(11)` invocation via `wp eval-file` on a real pending order → HTML produced (1974 bytes), no PHP fatal/error in `debug.log` | PASS (with a caveat) | **Caveat, stated honestly:** WooCommerce 7.4.0 (pinned in `.wp-env.json`) predates the 10.4 deprecation, so the original warning could not be reproduced/disproven directly in this playground. What WAS verified: the new code runs without error. What was NOT independently re-verified: that `wp_add_inline_script()` actually attaches and outputs the script tag on a real front-end page load — `wp_scripts()->get_data('woocommerce','after')` returned `false` when called via `wp eval-file` (CLI context never registers the `woocommerce` script handle the way a real page load does), so that specific assertion could not be made in this environment. Tagged `VERIFY` rather than claimed `IN PLACE` until confirmed on WooCommerce ≥10.4 or a real browser page load. |

| First automated test coverage — `RedsysLiteAPI` (HMAC signature creation/verification) unit tests | — | driven | — | yes — `npx wp-env run cli` (`cli` container, PHP 7.4) | mutation-tested for real: broke `mac256()`'s call site in the notif signature methods → 3/8 tests failed as expected; reverted (byte-identical `diff`) → 8/8 green again. Signature independently cross-checked against a from-scratch reference implementation of Redsys's documented algorithm (not the class under test); tamper and wrong-secret cases confirmed to change the signature. | not applicable — no UI | n/a — no strings changed | searched for an existing test scaffold first (none existed, confirmed in `docs/03-technical-plan.md` prior to this slice) | `docs/03-technical-plan.md` Testing section updated with the real commands and result; `docs/decisions.md` D-016/D-017 record the scope decision | n/a | `npx wp-env run cli bash -c "cd wp-content/plugins/woo-redsys-gateway-light && composer install"`; `npx wp-env run cli bash -c "cd wp-content/plugins/woo-redsys-gateway-light && vendor/bin/phpunit"` → `OK (8 tests, 8 assertions)` | PASS | Scope is `RedsysLiteAPI` only (true unit tests, no WP bootstrap — D-016); the gateway classes and notification handlers remain untested, tracked as an open deferred item |
| Second slice — `WC_Gateway_redsys::check_ipn_request_is_valid()` integration tests | — | driven | — | yes — `npx wp-env run tests-cli` (real booted WordPress + WooCommerce via WP core's own PHPUnit scaffold) | mutation-tested for real: replaced the `$localsecret === $remote_sign` comparison with an always-true condition → 3/5 tests failed as expected; reverted (byte-identical `diff`) → 5/5 green again. Automates the exact fail-closed scenario already driven manually in the "Adoption — playground verification" row above, plus forged-signature, tampered-payload-after-signing, and wrong-order-signature cases. | not applicable — no UI | n/a — no strings changed | reused `RedsysLiteAPI` itself (already tested) as the fixture builder for valid signed notifications, rather than duplicating signature logic in the test | `docs/03-technical-plan.md` Testing section updated with the real commands and result; `docs/decisions.md` D-018 records the scope decision | n/a | `npx wp-env run cli bash -c "cd wp-content/plugins/woo-redsys-gateway-light && composer update -W"`; `npx wp-env run tests-cli bash -c "cd wp-content/plugins/woo-redsys-gateway-light && vendor/bin/phpunit -c phpunit-integration.xml.dist"` → `OK (5 tests, 5 assertions)` | PASS | Only `WC_Gateway_redsys` covered; the other three gateway classes share the same `check_ipn_request_is_valid()` shape but are not individually tested yet |
| Third slice — `WC_Gateway_Bizum_Redsys::check_ipn_request_is_valid()` integration tests (real WC_Order fixture) | — | driven | — | yes — `npx wp-env run tests-cli` | mutation-tested for real: replaced the `$localsecret === $remote_sign` comparison with an always-true condition → 3/5 Bizum tests failed as expected; reverted (byte-identical `diff`) → 10/10 green again (both integration classes). | not applicable — no UI | n/a — no strings changed | reused `RedsysLiteAPI` as the fixture builder (as in the Redsys suite); reused the real `WCRedL()->prepare_order_number()` mapping mechanism (a `redys_order_temp_<Ds_Order>` transient) rather than inventing a new one | `docs/decisions.md` D-019 records the fixture approach and the L-003 correction | n/a | `npx wp-env run tests-cli bash -c "cd wp-content/plugins/woo-redsys-gateway-light && vendor/bin/phpunit -c phpunit-integration.xml.dist"` → `OK (10 tests, 10 assertions)` | PASS | While reading `class-wc-gateway-googlepay-redirection-redsys.php`'s full `check_ipn_request_is_valid()` to test it next, found it did NOT fail closed when no SHA-256 secret is configured (unlike Redsys/Bizum) — escalated to the user, fixed in the next row |
| Fourth slice — fixed the GooglePay signature-bypass vulnerability (D-020) and added `WC_Gateway_GooglePay_Redirection_Redsys::check_ipn_request_is_valid()` integration tests | — | driven | — | yes — `npx wp-env run tests-cli` / `npx wp-env run cli php -l` | regression-verified for real: temporarily reverted the fix (restored the pre-fix code from git), confirmed `test_rejects_the_notification_when_no_secret_is_configured` failed exactly as expected (the pre-fix code returned `true`), restored the fix, confirmed 15/15 integration tests + 8/8 unit tests green again. `php -l` on the edited file: no syntax errors. | not applicable — no UI | n/a — no strings changed | reused the exact fail-closed pattern (code + comment) already shipped in `class-wc-gateway-redsys.php`/`class-wc-gateway-bizum-redsys.php`, rather than inventing a new one | `docs/threat-model.md` "Known vulnerabilities" and the "Fail closed..." Defended-controls row updated to FIXED; `docs/decisions.md` D-020 records the fix | n/a | `npx wp-env run cli php -l wp-content/plugins/woo-redsys-gateway-light/classes/class-wc-gateway-googlepay-redirection-redsys.php` → no syntax errors; `npx wp-env run tests-cli bash -c "cd wp-content/plugins/woo-redsys-gateway-light && vendor/bin/phpunit -c phpunit-integration.xml.dist"` → `OK (15 tests, 15 assertions)` | PASS | Fix is on `develop`, unreleased — flagged in `docs/PROGRESS.md` as a reason to prioritize the next release |
| Fifth slice — `WC_Gateway_Inespay_Redsys::handle_callback()` integration tests (own signature algorithm + fixture) | — | driven | — | yes — `npx wp-env run tests-cli` | mutation-tested for real: replaced `if ( ! hash_equals( $expected_signature, $signature ) )` (the rejection guard) with `if ( false )`, so an invalid signature would never be rejected → 2/5 tests failed as expected (the forged-signature and tampered-after-signing cases); reverted (byte-identical `diff`) → 20/20 integration tests green again. | not applicable — no UI | n/a — no strings changed | own fixture builder (algorithm unrelated to `RedsysLiteAPI`); reused `WP_UnitTestCase`'s built-in `WPDieException`/`setExpectedIncorrectUsage()` machinery rather than inventing custom `wp_die()` interception | `docs/decisions.md` D-021 records the fixture approach; `docs/lessons-learned.md` L-004 records a pre-existing, out-of-scope `doing_it_wrong()` finding surfaced while writing the tests (not fixed) | n/a | `npx wp-env run tests-cli bash -c "cd wp-content/plugins/woo-redsys-gateway-light && vendor/bin/phpunit -c phpunit-integration.xml.dist"` → `OK (20 tests, 29 assertions)` | PASS | All four gateway classes now have IPN/callback test coverage; only a JS/checkout-flow (Playwright) suite remains as an open deferred item |
| Sixth slice — Playwright checkout smoke test (Redsys gateway, classic checkout) | — | driven | — | yes — real Chromium against `npx wp-env start`'s playground, `*.redsys.es` requests intercepted/aborted | mutation-tested for real: corrupted `DS_MERCHANT_MERCHANTCODE` in the generated form, confirmed the test failed on the expected assertion (`Expected: "999008881", Received: "WRONG"`), reverted (byte-identical `diff`), confirmed 3 consecutive clean passes. | driven — the actual rendered checkout page and generated form | n/a — no strings changed | none — new test surface (checkout flow had zero automated coverage before this) | `docs/playground.md` "Automated checkout smoke test" section (incl. the one-time environment setup it needed: pretty permalinks + a configured gateway, neither present by default); `docs/03-technical-plan.md` §Testing; `docs/decisions.md` D-022 | n/a | `npx playwright test` → `1 passed` (run 3 times) | PASS | Playground had two gaps this test surfaced: plain permalinks (`/checkout/` 404s) and no gateway configured by default — both documented as a one-time setup step, not scripted into `.wp-env.json` since they're playground *state*, reverted by `npx wp-env clean all`. Scope: classic `[woocommerce_checkout]` shortcode + Redsys gateway only — WooCommerce Blocks checkout and the other three gateways' checkout flows remain untested |

`Criterion` holds the acceptance criterion's stable `AC-nn` ID from
`docs/02-functional-spec.md` §6 (or `—` for a check that is not
criterion-bound). `Coverage` holds exactly one of: `driven`, or one of the
eight delegation tags from `references/test-automation.md` (`CREDENTIAL`,
`HARDWARE`, `ASSISTIVE-TECH`, `JUDGMENT`, `EXTERNAL-APPROVAL`,
`PLATFORM-IMPOSSIBLE`, `PRODUCTION-RISK`, `NO-EXECUTION`) — never free text.
