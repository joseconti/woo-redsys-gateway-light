# Changelog — Keel

All notable changes to the Keel skill are documented here.
Versions are listed oldest → newest (never inverted). Format groups: Added / Changed / Fixed.

## 1.0.0

Initial public release (GPL-3.0-or-later).

### Added
- Full idea-to-release multi-phase workflow with on-demand phase reference loading (progressive disclosure).
- Phase 1 Discovery: honest idea assessment (no default praise), project type fixing, installed-base/upgrade reality, fixed-version external dependencies, blocking multi-language vs single-language decision (function-wrapping and key/constant i18n models, base language as source of truth), project-website intent capture, design-needed decision.
- Phase 2 Functional Spec: flows, data model, integrations, permissions, design split (including external manual setup and assets Design can't produce), acceptance criteria.
- Phase 3 Design Handoff: brief enforcing build-once-reuse-by-manifest, ask-don't-invent, exact tokens, `external-setup.md` and `external-assets.md`, the shared handoff contract.
- Phase 4 Faithful Build: audit-first, consolidated BUILD-SPEC, zero-deviation build, one-step-at-a-time guided external setup, one-asset-at-a-time guided generation of images Design couldn't produce (adapting Design's base prompt to the user's chosen generator), two-branch failure handling, Design Request mechanism.
- Phase 5 Development: sprint planning, `docs/PROGRESS.md` living tracker, `docs/lessons-learned.md`, vertical slices with test points, live security profile, i18n build rule, migrations/backward-compat for installed base, fail-safe external dependencies, mandatory sprint close-out with archival to `docs/old/sprint-<N>/` and a self-sufficient continuation prompt for a fresh chat.
- Phase 6 Documentation: full `docs/` layout (architecture, API, usage, reference, security) with runnable examples; changelog ordering rule (oldest → newest).
- Phase 7 Release: `.gitignore` vs `.gitattributes` export-ignore boundaries, versioning, real-environment verification hard gate, release artifacts.
- Phase 8 Project Website (conditional): study the product → `docs/design/PRODUCT-BRIEF.md`; honest "is a site warranted?"; site type and the site's own blocking language decision; section catalogue & sitemap; dedicated-domain vs subdomain decision tree; design direction; vanilla-by-default (no frameworks/libraries/CDN unless a user-approved static-site-generator exception); self-hosted fonts with a guided procurement loop; product screenshots (Design reserves the slot, guided capture, build fits the CSS); technical SEO and AEO; real-environment launch checklist. Reuses Phases 3–7 rather than duplicating them.
- Per-platform security profiles: WordPress/WooCommerce, web app, MCP server, library/component.
- Shared templates: handoff contract, design brief, build spec, design request.
- Version reporting instruction and GPL-3.0-or-later licensing.

## 1.1.0

### Added
- Phase 8 Technical SEO & AEO: complete, verifiable spec for the full site-wide file set.
  - `robots.txt` with explicit per-bot policy for AI/answer-engine crawlers (ClaudeBot, PerplexityBot, OAI-SearchBot, Google-Extended, Applebot-Extended, GPTBot, anthropic-ai, CCBot, Bytespider, meta-externalagent) — the allow/disallow decision is the user's, recorded in discovery.
  - `sitemap.xml` with absolute URLs, `lastmod`, `xhtml:link hreflang` alternates for multilingual sites, sitemap index for large/multi-locale sites, image sitemap when warranted.
  - `humans.txt` referenced from the HTML.
  - `manifest.json` (web app manifest) with `theme_color`, icons including a maskable variant, self-hosted only.
  - `.well-known/security.txt` (RFC 9116) with `Contact`, future-dated `Expires`, `Canonical`, coordinated with the loaded security profile.
  - `llms.txt` (and optional `llms-full.txt`) as an AEO aid summarising the site for LLMs.
  - Favicon set: `favicon.ico`, `favicon.svg`, `apple-touch-icon.png`, manifest icons including a maskable variant.
  - Search-engine verification (Google Search Console, Bing Webmaster, IndexNow) when used.
  - Base `<head>` meta tag set explicit: charset, viewport, theme-color, color-scheme, referrer policy, format-detection, author.
  - Expanded JSON-LD coverage and rules: `WebSite` with `SearchAction`, `Organization`/`Person` with one canonical `@id`, `BreadcrumbList`, `WebPage`, `Article`/`BlogPosting`, `Product` with `Offer`, `VideoObject`, plus `SoftwareApplication`/`FAQPage`/`HowTo`. Inline-in-HTML rule, absolute URLs, consistency with rendered content.
  - Self-hosted fonts: `font-display: swap` and shipping only the actually-used weights/styles.
  - Compression, cache headers, HTTP/2 or HTTP/3, and resource hints (`preconnect`, `preload`) added as SEO-relevant transport items.
- Phase 8 Launch Checklist updated to fetch and verify every well-known file on the live domain, validate JSON-LD on live URLs, confirm the AI-crawler policy on `robots.txt`, and check self-hosted fonts in the network panel.
- Phase 8 Section Catalogue: site-wide deliverables (the well-known set) listed alongside page sections so they enter sitemap planning instead of being treated as launch-time extras.
- Three cross-cutting operating principles in SKILL.md:
  - **Reuse internal API; never duplicate code.** Search the existing API before writing new functions; generalize close fits instead of forking; duplication is a defect.
  - **Document every public surface at the moment it is created.** Each new function/method/class/hook/action/filter/REST route/MCP ability/CLI command is documented in `docs/api/` or `docs/reference/` as part of the same slice that introduces it; Phase 6 consolidates instead of writing from zero.
  - **Maximum extensibility for extensible project types** (WordPress/WooCommerce plugins, MCP servers, libraries/components): every user-facing string filterable, every meaningful decision exposes before/after actions, every query and response filterable, every public class replaceable. Decided at spec time, built into each slice.
- Phase 5 Development reflects the three principles operationally: per-slice checklist now includes search-before-writing, docs-at-creation, and extension-points-as-you-go; `docs/05-test-points.md` columns extended to record each; Definition of done enforces all three.
- Phase 6 Documentation rewritten as consolidation rather than creation. Undocumented public surfaces or missing extension points entering Phase 6 are Phase 5 defects to fix, not Phase 6 work.
- Phase 1 Discovery: new Step 0 — **Competitive scan**.
  - Always asks the user upfront which competitors they already know about.
  - Produces `docs/00-competitive-landscape.md` with per-competitor inventory (functionalities + license/pricing + status + cited sources), unified deduplicated feature list (de facto baseline for the category), and external-demand list (real user requests with citable sources).
  - Feeds `docs/01-discovery.md` with a "Competitive landscape & opportunity" section: table-stakes features, differentiator candidates, and optional AI/MCP/agentic layer proposals labelled as added-value or forced-filler (forced-filler dropped, not softened).
  - Cannot-research handling: when the environment lacks web/search, the assistant says so plainly with the specific reason and offers three options — move to a capable environment (preferred), use a different agent and bring findings back, or skip with an explicit warning block recorded verbatim in the discovery doc.
  - Definition of done updated; Phase 1 cannot complete without the scan being done or its absence explicitly recorded with the warning.

### Changed
- AEO section in technical SEO now explicitly references `llms.txt` and the per-bot `robots.txt` policy rather than only stating "don't block AI crawlers".
- SKILL.md phase map: Phase 1 description updated to "Competitive scan first, then idea, feature discussion, project type, constraints".
- Honest assessment in Phase 1 is now explicitly grounded in the competitive landscape (`docs/00-competitive-landscape.md`); without the landscape, the assessment is downgraded to opinion — the warning block makes this explicit.
- Version-change policy in SKILL.md hardened to unbreakable: version (`metadata.version`, heading line, `CHANGELOG.md`) is never bumped without explicit user instruction in the current conversation.

## 1.2.0

### Added
- Accessibility as a cross-cutting, non-negotiable concern applied on every platform and designed in from the first line — treated like security, and established up front in Phase 1 rather than retrofitted at the end.
  - New `references/accessibility.md`: a universal core (POUR non-negotiables, WCAG 2.2 AA floor with AAA where feasible, EN 301 549 / European Accessibility Act, honored user preferences) plus per-platform sections — Web/HTML, WordPress/WooCommerce, iOS/iPadOS, Android, macOS, Windows, and cross-platform frameworks — each with its native accessibility API, assistive technologies, and verification tooling. Includes the design deliverables, a phase map, and an honesty rule (no accessibility overlay, no unverified conformance claim).
  - SKILL.md: new operating principle, a cross-cutting "Accessibility" section parallel to Security routing (load `references/accessibility.md` once the project type and platform are fixed in Phase 1), a reference-index entry, and the description updated.
  - Phase 1 Discovery: a blocking "Accessibility commitment" step stated up front and paired with the i18n decision (retrofit = rewrite); target platform(s) captured; discovery-doc template and Definition of done extended.
  - Phase 2 Functional Spec: accessibility conditions required in every UI feature's acceptance criteria; per-screen accessibility requirements feed the design split.
  - Phase 3 Design Handoff, design brief and handoff contract: Design must deliver `SPEC/accessibility.md` and per-screen accessibility (contrast-verified pairs, focus order and visible focus, name/role/state per state, heading/landmark structure, target sizes, reduced-motion, error identification); a missing spec is a Design Request.
  - Phase 4 Faithful Build, BUILD-SPEC and Design Request templates: audit the accessibility spec, carry it into `BUILD-SPEC.md` (new §4a), and request it back when missing; faithfulness checklist and Definition of done extended.
  - Phase 5 Development: accessibility built into every UI slice with its own test point (automated + keyboard + real assistive technology); new accessibility column in `docs/05-test-points.md`; Definition of done extended.
  - Phase 6 Documentation: new `docs/accessibility.md` (applied measures, verification evidence, known gaps) consolidated, not invented at the end.
  - Phase 7 Release: accessibility verification is a release gate — automated checks plus a real assistive-technology pass on the actual distributable.
  - Phase 8 Project Website: web accessibility (WCAG 2.2 AA floor) is an explicit, verified deliverable checked with real assistive technology at launch, across the website orchestration, technical SEO, design direction, site discovery, and launch checklist references.
- README.md updated to describe accessibility as a cross-cutting, non-negotiable concern and to list `references/accessibility.md` in the repository layout.

## 1.3.0

### Added
- New cross-cutting reference `references/project-state.md` — the living state system, created at the first minute of Phase 1 (no longer in Phase 5):
  - `docs/PROGRESS.md` template: project card (type, stack, license, docs language, security profile, a11y level, i18n, installed base, website intent), phase-status table, exact current position with an executable "Next action", open items. Kept ~one page, updated at the moment of every change.
  - `docs/decisions.md`: append-only decision log — a session never re-litigates a recorded decision on its own initiative; reversals are appended by the user's call.
  - `docs/lessons-learned.md`: append-only problem→solution log with a "rule for next time" per entry.
  - Design Request register: every DR saved as `docs/design/design-requests/DR-NNN.md` with a Status line; "zero unresolved Design Requests" is verified against the register, never against memory.
  - `docs/api/INDEX.md`: one line per public surface — the cheap lookup consulted by the reuse rule before opening full API docs.
  - Sprint file template, archiving rules (what moves to `docs/old/`, what never moves), and a universal continuation prompt usable from ANY phase (not only sprint closes).
  - Context & cache discipline: fixed session-start reading order (PROGRESS → decisions → lessons → phase reference → named inputs), read each static reference once per session, orient by recorded state instead of scanning code, surgical code reads, small living state vs stable artifacts, reference paths instead of duplicating content.
- Phase 2: new step 4 "Technical foundation" producing `docs/03-technical-plan.md` — stack with exact versions and rationale, support matrix and budgets, architecture and persistence, code map (future sessions orient here, never by scanning the tree), conventions (prefix/namespace, naming, error handling, logging), testing strategy with exact run commands and the regression rule (every fixed bug gets a pinning test), tooling commands, version touchpoints, license and dependency compatibility. Consumed by the Phase 3 brief ("where the final code will live") and by Phase 5 as the build's rulebook.
- Phase 1: license decision (constrains dependency adoption from Phase 5 and marketplace requirements; shipping verified in Phase 7); docs-language decision (every docs/ artifact in one language across all sessions); state files initialized before any artifact, confirming the project directory with the user first.
- Phase 1: new step 9 — **design system / brand identity decision**, made before Design ever starts. Establishes whether a design system already governs the brand (existing → source/location recorded and binding: Design applies it, deviations are questions, and the Phase 4 audit flags unexplained token divergence as a gap), whether this project **founds** the brand's canonical system (recorded home for future projects to inherit), or is an intentional one-off. Includes a **founding interview** (batched questionnaire) whose answers seed the Phase 3 brief: logo (existing files, or **Design creates it** — delivered as a master SVG with horizontal/stacked/icon-only, on-light/on-dark and monochrome variants, minimum sizes and clear space, indexed in `assets-index.md`), colors loved/vetoed, typography preference with licensing constraints, brand personality, liked/disliked references, dark-mode intent, iconography/imagery style, and explicit vetoes; unanswered items go to `SPEC/open-questions.md` for Design to ask, never to guess. Propagated across the design brief template (design-system status + founding seed + logo deliverable in Section 2), the handoff contract (`SPEC/design-tokens.md` states its origin: derived / founded / one-off), Phase 3 inputs and definition of done, the PROGRESS.md project card, and Phase 8 (the site inherits the brand's design system by default; a site-specific identity is an explicit recorded decision — and if the product had no UI, the site's design founds the system).
- Phase 5: toolchain verified end to end at scaffold (exact lint/test/build commands on a trivial passing test before slice 1); commit discipline (commit at minimum at every passed test point, repo never left broken, git history is part of the resumable state); regression tests linked from lessons-learned entries.
- Phase 7: version-touchpoint sync — every location listed in the technical plan (plugin header, readme.txt Stable tag, VERSION constant, package.json) updated and verified identical; license file/headers verification including bundled dependency compatibility; WordPress.org release metadata specifics (readme.txt Requires at least / Tested up to / Requires PHP / Stable tag, main-file header sync, listing assets).
- Phase 8: explicit "where the site lives" decision — own repository (own Keel docs/ tree and state) or inside the product repo under a mandatory `docs/site/` namespace, preventing the site handoff from colliding with the product's `docs/design/design-handoff/`; `<site-docs>` notation adopted across the phase-8 references; analytics & consent decision in site discovery (none by default / self-hosted / third-party only as an explicitly approved vanilla exception; EU consent requirements make cookie/privacy sections mandatory when tracking exists) verified in the launch checklist's network panel.
- Security profiles: WordPress adds a "Common WP pitfalls" block (ABSPATH direct-access guard in every PHP file, `wp_safe_redirect` for input-derived redirects, `$wpdb->esc_like` before prepare, real `sanitize_callback` on `register_setting`, capability+nonce re-checks on cron/admin-post/admin-ajax) with matching test points; web app adds SSRF protections (allow-list destinations, block internal ranges/metadata endpoints) and server-side upload handling.
- New reference `references/adoption.md` — the third entry mode (alongside new project and resume): **adopting Keel in a project already underway**. Read-only inventory of what exists (stack, released version and installed base, tests run once, public surfaces, hygiene, version touchpoints); state initialized with de facto decisions recorded as "adopted as-is"; the never-made Phase 1 decisions asked in batches (type/security/accessibility, license, i18n, docs language, design system, website intent; competitive scan recommended-but-optional); artifacts reconstructed **as-built** with unverified inferences labeled (`01-discovery`, `02-functional-spec` with progressive backfill, `03-technical-plan` with observed — not imposed — conventions, complete `docs/api/INDEX.md`); no retroactive BUILD-SPEC invented for pre-Keel UIs (baseline recorded; next UI change follows Phases 3–4); gap audit against Keel's standards into `docs/04-adoption-audit.md`, prioritized with the user (fix now / fix when touched / accepted); adoption changes no code except a user-approved critical remediation (e.g. a tracked secret). SKILL.md gains an explicit "Entry modes" section, the description triggers on applying Keel to existing projects, and the Phase 6 numbering map covers `04-adoption-audit.md`.
- **Portability lock** (`references/project-state.md` "Portability"): every Keel project carries a `CLAUDE.md` block at the repo root — created in Phase 1 step 0a and in adoption — that binds ANY assistant/environment opening the repo (Claude app, Cowork, Claude Code in VS Code, other AIs via an optional `AGENTS.md` mirror) to the Keel workflow: read state first, follow the skill or the embedded copy literally, never re-litigate decisions, update state at the moment of change, and stop if neither the skill nor the embedded copy is available. Delimited (`KEEL:BEGIN`/`KEEL:END`) so it can be updated without touching the rest of the user's CLAUDE.md. Optionally (asked once, recorded in the project card) the skill is **embedded** at `.claude/skills/keel/` — Claude Code loads it as a project skill automatically, other environments read it as files, and the repo becomes self-sufficient; the embedded copy is version-synced from the installed skill in one direction and never hand-edited. `CLAUDE.md`, `AGENTS.md` and `.claude/` are export-ignored in Phase 7 so workflow files never ship in the distributable.

### Changed
- SKILL.md: two new operating principles — keep the living state current from the first minute, and work from recorded state / read code surgically (fixed reading order, cache-friendly sessions); "How to run a phase" updates state as work happens, reports every phase's definition of done item by item as an explicit ✓/✗ checklist before advancing, defines project-start state initialization, deterministic resume via `references/project-state.md`, and a session-end continuation prompt from any phase; the skill description now also triggers on resuming an in-progress Keel project (a repo containing docs/PROGRESS.md); the version-change policy is clarified to govern Keel's own version only (projects built with Keel follow Phase 7).
- Phase 3: inputs now include `docs/03-technical-plan.md` (the brief's target stack comes from it, never guessed).
- Phase 4: Design Requests are registered on disk before being handed over; Steps 4–6 may interleave when the build requires it (same rules, different order — each step/asset still one at a time, verified, traced); the audit includes `SPEC/screenshots.md` for website projects; definition of done includes a clean DR register.
- Phase 5: step 0 extends the Phase 1 state files instead of creating them, and confirms the technical plan; the reuse rule consults `docs/api/INDEX.md` first; per-slice docs duty includes the INDEX row; sprint close leaves the repo clean and green, follows the shared archiving rules, and producing the continuation prompt no longer forces a chat switch.
- Phase 6: canonical `docs/` layout completed (`00-competitive-landscape.md`, `03-technical-plan.md`, `decisions.md`, `api/INDEX.md`, `design/design-requests/`) with a numbering map explaining the gaps; `architecture.md` consolidates from `decisions.md` and the technical plan; a short repo `README.md` (front door linking into docs/) is a deliverable; INDEX must match the docs one-to-one.
- Handoff contract and shared templates: stale references to the absorbed standalone skills (`design-spec-handoff`, `code-faithful-build`, "see skill Step 5/6") replaced with Phase 3 / Phase 4 language; the contract documents the website-only `SPEC/screenshots.md` addition; the design brief template fills from the Phase 1–2 artifacts; the design request template carries its DR number and status line.
- Phase 8 sub-references record into the site's own discovery/spec docs per the namespace decision; the domain decision is also logged in `docs/decisions.md`.

### Fixed
- Resume no longer depends on a file that did not exist before Phase 5: `docs/PROGRESS.md` exists from Phase 1 on every Keel project, so a chat dying in Phases 1–4 resumes deterministically.
- Phase 6's canonical layout no longer omits artifacts earlier phases create (competitive landscape, state files), which could have led a literal session to archive or distrust them.
- README repository layout lists `INSTALL.md` and `references/project-state.md`; INSTALL.md's verification example no longer hardcodes an outdated version number.

## 1.4.0

### Added
- Real functional verification as a cross-cutting principle: whenever the project can be run, a runnable **verification playground** (Docker/docker-compose, wp-env, a playground script, a disposable sandbox — whatever fits the stack) exists alongside the automated test suite, so the assistant exercises the software for real — full flows end to end, the CLI if one was built, real API calls. Automated tests prove the parts; the playground proves the product. The user gets to try it too: access details when needed (URL/host, user, password — local, throwaway credentials only, never production secrets) plus step-by-step try-it instructions, maintained in `docs/playground.md`.
  - SKILL.md: new operating principle.
  - Phase 2: the technical plan's Testing section defines the playground and its exact start/stop commands.
  - Phase 5: new principle; the playground is stood up at the scaffold (software verified to load in it) and `docs/playground.md` created there; every test point exercises the slice in it for real (new confirmation (e)); the cross-cutting verification runs the full pass in the playground and invites the user to try the flows; new "Real run in playground" column in `docs/05-test-points.md`; Definition of done extended.
  - Phase 6: `docs/playground.md` added to the canonical `docs/` layout and the numbering map.
  - Phase 7: the real-environment verification gate may reuse the Phase 5 playground when it is of the correct type — installing the exact distributable, never the dev tree — with `docs/playground.md` as the user's guide for their own final pass.
  - README.md updated (phase table and operating principles).

### Changed
- SKILL.md frontmatter description shortened to 999 characters to fit the 1024-character limit enforced by skill installers; all trigger phrases preserved.

## 1.5.0

### Added
- **Output language & internationalization contract (English by default, always).** New cross-cutting section in SKILL.md plus two operating principles: the base/output language of everything built (source strings, UI copy, identifiers, commit messages) defaults to **English** in every project, never Spanish. At the start of every project the i18n questions are asked explicitly (multi- vs single-language, which output locales, is English the base). WordPress/WooCommerce is a fixed rule — base language always English and always built multi-language-ready — never Spanish-base. The docs language stays the user's working language (normally Spanish), a separate decision.
  - Phase 1 Discovery Step 6 rewritten from "internationalization decision" to "internationalization & output language", with the three explicit questions, the WordPress/WooCommerce fixed rule, the English-by-default base, and the discovery-doc template + definition of done updated.
  - Phase 5 Development: the i18n build rule now states the base language is English by default (always English for WordPress/WooCommerce), and the definition of done verifies an English `.pot` regardless of conversation language.
- **Writing quality — perfect orthography (unbreakable).** New cross-cutting section in SKILL.md plus an operating principle: everything written, in any language and on any surface, must be orthographically and grammatically perfect. Spanish specifically must carry every accent/tilde, every ñ and the opening ¿/¡, with zero spelling or grammatical errors — treated like a code bug, never shipped.
- **Design assets are delivered build-ready — the build never transforms them.** New rule across the handoff chain: every logo and icon ships in **both SVG and PNG** (PNG at intrinsic size plus the platform's required densities/sizes), and every asset is delivered in a format the target build uses directly, so Code never has to convert, resize, recolor, rasterize, or re-export. An asset that would force a build-side transformation is an incomplete handoff (a Design Request).
  - `handoff-contract.md` rule 4 expanded; folder-structure note and "what complete means" updated.
  - Phase 3: new core principles (build-ready assets; every screen defined by what it DOES, not just how it looks), brief requirements and definition of done updated.
  - Phase 4: Step 1 reframed as the completeness gate run FIRST (verify Design delivered everything without exception; anything missing becomes a registered Design Request, never a build-side workaround); asset-format audit check added; faithfulness verification extended.
  - `design-brief-template.md`: logo in SVG+PNG, icons in SVG+PNG, build-ready formats requirement, per-screen "what it does", definition of done updated.
  - `build-spec-template.md`: asset map gains SVG+PNG and build-ready columns; faithfulness checklist gains design-system fidelity and asset-format items.
  - `design-request-template.md`: assets category covers missing formats (SVG/PNG) and non-directly-usable formats.
- **Complete skill copy (portability lock).** `project-state.md` embedded-skill rules hardened: copy the whole skill (every file), verify against the source manifest, retry on any gap, verify again, and if it still fails STOP and tell the user exactly which files did not arrive so they can move them — never leave a half-copied skill as if it worked. Version sync follows the same verify → retry → tell-user protocol.

### Changed
- SKILL.md operating principle "Code adapts to the design, never the design to the code" expanded to state the build follows the design to the letter and the code strategy changes (logged), never the design intent — enforced through the handoff contract.

## 1.6.0

### Added
- **Token economy — everything Keel creates defaults to English (READ FIRST).** New section at the very top of SKILL.md: every artifact Keel creates — the whole `docs/` set (discovery, specs, progress, decisions, lessons learned, architecture, API reference, playground instructions), continuation prompts for new chats, prompts and briefs handed to Claude Code or Claude Design, template instances, reports, commit messages, code comments — is written in **English by default**, because English is the most token-efficient language for an LLM and the living state is re-read in every session, so the saving compounds for the life of the project. The default is announced to the user in one line at the start of every project (and adoption); any other language is available on request, with the increased token consumption — and therefore cost — made explicit and the choice recorded in the project card and `decisions.md`. On resume/adoption of a project whose docs already exist in another language, the user is asked once whether to translate everything to English; the answer is recorded and never re-asked. The policy does not change the conversation language (the assistant keeps talking in whatever language the user writes) and never drops the product's shipping locales.

### Changed
- SKILL.md: the output-language operating principle and the "Output language & internationalization" contract no longer default the docs to the user's working language — docs and every other created artifact default to English (token economy); the Phase 1 §6 question batch gains question 4 (docs language — confirm English or record an explicit off-English choice with the trade-off acknowledged).
- Phase 1 Discovery §6: the docs-language decision rewritten to English-by-default with the one-line trade-off statement when asking; discovery-doc template and definition of done updated accordingly.
- `project-state.md`: the project card's docs-language line carries the English default, and the continuation prompt is written in English regardless of the conversation language.
- `adoption.md`: the docs-language question now states the English default and includes the one-time offer to translate existing non-English docs to English.

## 1.7.0

### Added
- **Estimation & budget — AI-time based.** New cross-cutting reference `references/estimation-budget.md`: when the user needs to quote a project (or a feature) to a client, Keel produces a realistic, itemized estimate and a client-ready budget computed from how the work is actually delivered — the **AI's working hours** (session wall-clock per phase, scaled by the recorded scope) plus the **vibe coder's supervision hours** (answering question batches, making decisions, real-world testing the AI cannot do, external setup, uploading code, client bridge) — **never from traditional human development time**. Itemized lines are mandatory (segment | what is done | hours; totals derived from the lines; visible contingency, +20–30% default).
  - Two fixed moments: **preliminary estimate at Phase 1 close** (wide ranges, so the user can answer the client early) and **firm estimate + client budget at Phase 2 close**; recomputed as a new, re-approved version on any recorded scope change; also runnable on adopted-project work.
  - Batched runtime questions: hourly rate + currency, AI access mode (subscription ≈ 0 marginal cost vs API pay-per-token) and model(s), contingency, the budget's language (a client-facing deliverable, asked per project — the internal estimate stays English per the token economy), taxes note, availability for the calendar estimate.
  - AI cost per model: token estimate by project size with the input/output split and the prompt-caching effect; **prices always verified online before quoting** (dated fallback table included, verified July 2026); on subscription the default recommendation is not to bill the AI as a line item, since it is not an extra expense.
  - Artifacts: `docs/estimate.md` (internal, English, versioned preliminary → firm) and `docs/budget.md` (client's language, developer block and AI block SEPARATE, priced line by line, totals, estimated calendar delivery, terms), plus a mandatory **present → adjust → approve** loop with the user, recorded in `docs/decisions.md`.
  - SKILL.md: new operating principle ("Budgets are AI-time based, never human-time based") and a cross-cutting "Estimation & budget" section; Phase 1 gains step 10 and Phase 2 gains step 7, with their doc templates and definitions of done extended; the PROGRESS.md phase-table artifacts include estimate and budget.
- **Token ledger (`docs/token-ledger.md`).** Actual token usage recorded as the project runs: one row per working session (date, phase/sprint, model(s), input/output tokens, method — measured via environment counters/API usage/provider dashboard, or honestly estimated from volume when nothing is exposed; the method is always stated). Created with Estimate v1, appended at session ends, verified at phase/sprint closes; at release Phase 7 runs the **final reconciliation** — totals by model, cost at verified prices, deviation vs the estimate, and the calibration lesson — so every finished project makes the next estimate better. Added to the state-file table, Phase 5 sprint close and definition of done, Phase 7 steps/template/gates, and the Phase 6 canonical layout.
- **Forge issue log (`docs/issues.md`).** Whenever the project's issues on its Git forge — GitHub, GitLab, Gitea, Bitbucket, or any other — are accessed or worked, a single living log records the full picture at the moment it changes: an **inventory** (number, title, type, priority, status) plus **one entry per issue worked** (link, status, diagnosis/root cause, resolution approach, commits/PR and the shipping version, verification including the regression test, linked lesson, pending items) — so if a problem surfaces later, what was done and how is on record, never reconstructed from memory. Template and rules in `references/project-state.md` (update at the moment of change; both-directions rule; old resolved entries may archive to `docs/old/issues-archive.md` — move, never delete). Integrated across SKILL.md (new operating principle), Phase 5 (per-slice rule, sprint close, definition of done), Phase 7 (issues closed by the release, `07-release.md` template, gate), adoption (forge inventory + initial log), the Phase 6 layout, and the PROGRESS.md open-items line.

### Changed
- SKILL.md frontmatter description gains the new triggers (what a project will cost or take — quote/budget; working forge issues) while staying within the 1024-character installer limit; the phase-map rows for Phases 1–2 now mention the preliminary estimate and the firm estimate & client budget.
- `README.md` version line realigned (it still said 1.4.0) and the readme extended with the new features and the `references/estimation-budget.md` entry in the repository layout.

## 1.8.0

### Added
- **Execution discipline in the `CLAUDE.md` lock.** The portability lock's protocol block (`references/project-state.md`, "Portability") gains item 5: mandatory execution discipline for ANY assistant/model working in a Keel repo, in any environment. Six rules: batch independent tool calls in ONE parallel block (never run sequentially what does not depend on a previous result); delegate broad searches/scans to a subagent when the environment provides them, bringing back conclusions — never file dumps — so the main context stays clean; no narration between tool calls ("now I will…") — findings accumulate and are reported once, at the end of the work block; locate before reading (search/grep first, then read only the relevant fragment — never whole files or directories "for context"); edit surgically with exact-match edits on the changed lines instead of rewriting whole files; batch clarifying questions at the START of a work block and close every work block with an explicit verification step (diff, test, or re-read) before calling it done. Because the block lives between the `KEEL:BEGIN/END` delimiters, existing projects pick it up through the normal lock-refresh mechanism (with the user's OK).
- **Release update check at session start.** New SKILL.md section "Update check (start of every session)": once per session, before the entry-mode decision, Keel checks its distribution repo (`github.com/joseconti/keel-skill`) for a newer release — preferred method `git ls-remote --tags` (highest semver tag, numeric segment comparison), falling back to the GitHub latest-release API or the releases page — and compares it with the frontmatter version of EVERY copy in play, not only the running one — the environment's install and, when the session works inside a project that embeds the skill, the project's `.claude/skills/keel/` (in Cowork it is common that the app install is current while the opened project's embedded copy is behind; that copy must still be updated, copying from the current local install when one exists instead of downloading). Keel then updates EVERY copy the environment can durably write — the user-level skills install and, always when present, the project's embedded `.claude/skills/keel/` (the normal case when Claude Code or Cowork is working inside a Keel project's repo — the same duty that put the embedded copy there also keeps it current): full-tree replacement under the verified full-copy protocol of `references/project-state.md`, improvements summarized from the new CHANGELOG, then continues under the new version. For a copy it cannot update durably (app-managed or ephemeral skill storage, common in the Claude app / Cowork), it informs the user once — new version, its improvements, and how to update per `INSTALL.md`, noting when the embedded copy was already brought current — and continues with the running version. The check is best-effort and never blocks or delays work; installing an official release is an installation, not a version edit, so it does not fall under the version change policy (scope note updated accordingly).

## 1.9.0

### Added
- **Confidential data never reaches Git (cross-cutting, UNBREAKABLE).** New SKILL.md section plus an operating principle: before EVERY commit and push — test points and sprint closes (Phase 5), the release (Phase 7), website deploys, adoption's first commit, any ad-hoc commit — the staged files are checked for confidential data, by name (`.env*`, key and certificate files, `id_rsa*`, `wp-config.php` with real values, database dumps and exports, backups, credential files) and by content (private-key blocks, API keys and tokens, passwords in config, OAuth client secrets, payment-gateway merchant keys, license keys, real personal or customer data in fixtures/seeds/logs/dumps), using a dedicated scanner (`gitleaks`, `trufflehog`) when available. A finding STOPS the commit: the user is warned file by file that letting it reach the repository is a serious security risk, and the fix is applied before anything is committed — `.gitignore` exclusion (never tracked), untrack plus ignore (tracked but never pushed), or history purge (`git filter-repo` / BFG) plus credential rotation (ever pushed: the credential is compromised). The user can explicitly accept a flagged file (placeholders, sandbox-only keys meant to ship), recorded in `docs/decisions.md`. Integrated end to end: the `CLAUDE.md` lock's item 4 now forbids committing without the check (existing projects pick it up through the normal lock-refresh mechanism), Phase 5's commit discipline runs it at every commit, and Phase 7 runs it over the WHOLE tracked tree — not only recent changes — before release, rotating anything that was ever pushed.

## 1.10.0

### Added
- **Native Claude Code project configuration — the optional Claude config package (`references/claude-config.md`).** Keel can now generate, per project and only with the user's consent, the configuration Claude Code loads natively, derived exclusively from decisions the project has already recorded: path-scoped `.claude/rules/` (code style from the technical plan's §Conventions, the distilled security profile, and Keel's build discipline — each under ~40 lines, pointing to its `docs/` source instead of duplicating it), `.claude/agents/` reviewer subagents (`code-reviewer`, `security-auditor`, `docs-verifier` — read-only tools, no pinned model, they flag and never fix), `.claude/settings.json` with a minimal permission allow-list built ONLY from the plan's verified tooling/playground commands and ALWAYS confirmed by the user before writing, a **confidential-data git pre-commit gate** (`.githooks/pre-commit` + `core.hooksPath` — a classic git hook chosen deliberately over an assistant-specific hook because it fires in every environment and editor; installed at the scaffold and VERIFIED by blocking a synthetic secret; conscious, on-the-record bypass policy), and `.mcp.json` only when the technical plan defines development MCP servers (environment expansion, never a literal secret).
  - Offered ONCE in the Phase 1 step 0a batch (alongside the embed-the-skill question) and at adoption step 2; recorded in the project card as `Claude config: [none / rules / rules+agents / full]` with its D-entry. Rules and agents materialize at Phase 2 close, when their sources are fixed (adoption: after its step 4, encoding the OBSERVED conventions, never imposed ones); settings, the gate, and `.mcp.json` at the Phase 5 scaffold.
  - Position in the workflow: the `CLAUDE.md` lock remains the universal mechanism — only Claude Code loads rules/agents/settings, so nothing critical to the workflow lives only in `.claude/`; the package is reinforcement and ergonomics for Claude Code sessions, never a replacement. Token economy applies throughout: rules are path-scoped by default and reference their `docs/` sources instead of restating them.
  - `CLAUDE.local.md` and `.claude/settings.local.json` are never created by Keel (they are the user's personal files) but are ALWAYS added to the scaffold's `.gitignore` — this entry is unconditional, even when the whole package is declined.
  - Integrated end to end: SKILL.md (shared templates, reference index, entry-modes portability note), Phase 1 (step 0a offer + definition of done), Phase 2 (new step 4a + definition of done), Phase 5 (scaffold completion + definition of done), Phase 7 (`.gitignore` gains the personal assistant-config entries; export-ignore extended to any generated Claude config plus `/.githooks` and `/.mcp.json`; pre-release verification that the package still matches the recorded decisions and the gate still blocks), adoption (step 2 offer — gate and `.gitignore` entries installable immediately; existing `.claude/` config is inventoried and reconciled, never overwritten — step 4 materialization, definition of done), `project-state.md` (state-file table rows, project-card `Claude config:` line, Portability §3), and `README.md`.
- **Post-update reconciliation — a Keel update now brings the open PROJECT up to date, not only the workflow.** New `references/project-state.md` section: after the session-start update check replaces any copy — and on resume, whenever the project card's `Keel baseline:` is older than the running version or missing — the session re-reads the new `SKILL.md` AND the current phase's reference from the new copy (the copies in context belong to the old version; the one exception to the read-once rule), reads the new `CHANGELOG.md` entries after the project's baseline, and extracts what touches the PROJECT itself: files/directories the project should now have, project-card lines that now exist, lock-block changes between the `KEEL:BEGIN/END` delimiters, questions a phase now asks that this project was never asked, new one-time verifications. It then presents ONE batched catch-up plan for the user to approve, trim, or defer — a deferred reconciliation is recorded in PROGRESS.md open items so no session forgets it. Applying it never re-opens recorded decisions (conflicts are surfaced; the recorded decision wins until the user reverses it), never re-runs completed phases, and never force-installs optional mechanisms — it asks their never-asked question (e.g. the Claude config package for a project created before v1.10.0). Everything is recorded: one D-entry per reconciliation and the new project-card line `Keel baseline:` (stamped at Phase 1 step 0a / adoption step 2 with the running version, advanced ONLY by completing a reconciliation — a skill update alone never advances it).

## 1.11.0

### Added
- **The `CLAUDE.md` lock now opens every session: step 1 is reading the FULL SKILL.md.** Field-tested gap: on a fresh chat, an assistant that already had the lock in context would jump straight into the state files (the old step 1) and never load the skill body — so the session-start update check never ran, and the user had to request a full re-read by hand. The lock block is reworked: **step 1** is now to read the FULL `SKILL.md` — installed skill or embedded copy — before anything else in the repository, starting with its session-start update check; remembering the protocol from an earlier chat, or having the lock in context, does not count as having read it. When the check installs a newer Keel, the lock itself orders the **post-update reconciliation** BEFORE normal work continues, so the project is brought up to date with everything the new version requires (new files or directories, new project-card lines, the lock block itself, never-asked questions). **Step 2** is then the state files — `PROGRESS.md`, `decisions.md`, `lessons-learned.md`, plus the current phase's reference — with the `Keel baseline:` comparison. The update-check section states the same cue from its side ("reading this section IS the cue"; the lock makes this its step 1). Existing projects pick the new block up through the normal lock-refresh mechanism or the post-update reconciliation.

- **Lock freshness check — stamp-only, one-line verification; the always-loaded rules can no longer drift behind the skill.** The `KEEL:BEGIN` delimiter now carries a version stamp naming the Keel that last wrote the block (`KEEL:BEGIN — vX.Y.Z do not remove: …`), and SKILL.md's update check gains a companion step: every session inside a Keel project checks the stamp against the running version — equal → current, nothing else to read; different or missing (pre-1.11.0 blocks have no stamp; delimiters are matched by the `KEEL:BEGIN` prefix, never by exact text) → rewrite the block between the delimiters from the canonical copy in `references/project-state.md`, restamped with the running version, with the user's OK. The stamp alone decides — never a content comparison, by design. The block itself states the same rule from its side, so an assistant that only has the lock in context knows the block may be outdated and where the canonical copy lives.

### Fixed
- **Confidential-data gate: field-tested false-positive hardening.** Committing the gate's own script or the embedded skill tripped the gate — their files legitimately contain the very patterns it searches for — forcing a conscious bypass on the first real-world commit. Three measures: (1) the hook now exempts the canonical trees `.githooks/` and `.claude/skills/`, while the assistant-side check still scans them like everything else; (2) the synthetic-secret verification example in `claude-config.md` — and SKILL.md's own by-content pattern list, dating from 1.9.0 — rewritten so no canonical skill file contains a matchable secret-shaped string (verified by running the gate's pattern over the whole tree: zero matches); (3) a new writing rule, SKILL.md "Confidential data never reaches Git" point 5, mirrored in the decisions template: docs, decision notes, and lessons never embed a literal secret-shaped string — describe it or split it apart.

## 1.12.0

### Added
- **`MANIFEST.md` — the parity manifest: one file that says what a Keel project must contain, and which skill files changed in which version.** Read after every skill update (it is the post-update reconciliation's first input) and usable any time for a full audit. Two tables: (1) the PROJECT table — every directory and file a Keel project must have, with its template source, the phase that requires it, and its condition (always / UI / runnable / Claude config accepted / forge touched / adoption) — run as an ABSOLUTE, phase-aware parity check, so anything missing joins the reconciliation's batched plan no matter which version introduced it; (2) the SKILL table — every file of the `keel/` tree with the Keel version that last changed it, so a session knows exactly which references to re-read after an update: every row newer than the project's `Keel baseline:`. Maintenance is part of every release (per-file rows always; Table 1 whenever structure requirements change), and the manifest header becomes the FOURTH synced version location alongside the frontmatter, the SKILL.md heading, and the changelog — governed by the same unbreakable version policy. Authority rule: the phase references are the contract; a manifest that disagrees is a release bug to fix in the manifest.

## 1.12.1

### Added
- **Update-check throttle — at most one remote check per 24 hours per project.** Field-tested friction: with the lock forcing a full SKILL.md read at every session start, the remote release lookup (`git ls-remote` / GitHub API) ran in every new chat and delayed the start of work. Now a tiny machine-local stamp at the project root — `.keel-update-check`, one line with the UTC timestamp of the last attempt and its outcome — throttles it: less than 24 hours old → the remote lookup is skipped silently; missing, unparsable, or 24+ hours old → run it, then rewrite the stamp (after EVERY attempt, success or failure, so a flaky network cannot re-impose the wait on every chat). The throttle covers only the remote lookup: the installed-vs-embedded copy comparison, the full SKILL.md read, the stamp-only lock-freshness check, and the `Keel baseline:` comparison all still run every session — local and free. An explicit user request to check for a new version always bypasses the throttle. The stamp is never committed: it joins `CLAUDE.local.md` and `.claude/settings.local.json` as an unconditional `.gitignore` entry (Phase 5 scaffold, Phase 7 list, `claude-config.md` and MANIFEST Table 1 updated).

## 1.13.0

### Added
- **Vague-idea onramp (Phase 1 step 1) — a vague idea is a valid entry.** Keel now explicitly serves the user with no development background who arrives with something as thin as "I want something to organize my recipes": when the idea is vague or the user cannot answer the understand-the-idea questions in their own terms, the assistant does not interrogate — it proposes 2–3 concrete interpretations of the idea (grounded in the step 0 scan where it helps), each described in one plain-language line, and the user picks or corrects one. Everything inferable is inferred; the phase never stalls on a question the user cannot answer — it becomes options with a recommended default.
- **Proposed v1 — the assistant proposes, the user reacts (Phase 1 step 3, replacing "Feature discussion").** The user is never asked to build a feature list from a blank page. From what Phase 1 has already produced — the unified feature list (table stakes), the external-demand list (differentiator candidates), the AI/MCP added-value proposals, the honest assessment, and the user's own idea — the assistant assembles a proposed v1 and presents it unprompted, always, as a draft to react to, never as a decision already made: a feature table where every row carries its why (`table stakes (competitors X, Y)` / `differentiator (source)` / `AI/MCP added value` / `user's idea`), an explicit "Later" list so cutting is visible and painless, and a tight-by-default scope with removal explicitly invited — the proposal is a starting point, not an anchor, and the user's corrections always win. If the user arrived with a defined feature list or scope of their own, the assistant presents the diff against the scan instead (missing table stakes, items that belong in Later, differentiator candidates they may want) — never a re-proposal from scratch. The discovery template gains a "Why in v1" column and the definition of done now requires the proposed v1 (or the diff) to have been presented.
- **Every question answerable by a non-developer (SKILL.md "How to run a phase" §3).** Every question Keel asks the user carries a recommended default and a one-line plain-language explanation of what it means and why the default is sensible. "I don't know / whatever you think" is a valid answer: the default is recorded in `docs/decisions.md` as "default accepted" and the work moves on — a phase never stalls on a question the user cannot answer.

### Changed
- SKILL.md trigger description now names the vague-one-line-idea, no-technical-background entry (Keel shapes it and proposes the v1 unprompted), and the phase map row for Phase 1 reads "proposed v1 (assistant proposes, user reacts)" instead of "feature discussion".

## 2.0.0

Major release: **verification becomes executable, and the lifecycle no longer ends at the release.** Gates that were prose now demand evidence — commands with outputs, diffs, recomputed values, independent reviewers with fresh context — and the skill itself gains its own test, lint and CI infrastructure so the failure classes its own changelog documents (version drift, field-tested mechanism gaps) get caught before a tag instead of in the field.

### Added
- **`references/keel-maintenance.md`** — the update check, its 24-hour throttle, the lock-freshness check, version reporting, and the full UNBREAKABLE version change policy, extracted from the SKILL.md body (which drops roughly a third of its per-session weight). SKILL.md keeps a binding "read and execute first" block plus the policy's core paragraph, so the guarantee of execution survives the extraction. The throttle stamp now carries a consecutive-failure count, and at the fifth consecutive failed remote lookup the user is told once, briefly.
- **`references/playground-recipes.md`** — per-platform verification environments, operable by the assistant so "exercise it for real" means commands with recordable output: wp-env pinned to the support matrix with WP-CLI smoke checks and Woo seed data; MCP Inspector plus registering the dev server in `.mcp.json` so the assistant calls the tools live; local server or docker compose with Playwright e2e for web; a clean consumer project installing the BUILT artifact for libraries. Synthetic seed data with a reset command is part of every recipe (an empty store proves nothing), and "gate zero" (clean build + lint + one trivial passing test) opens every scaffold.
- **`references/maintenance.md`** — the post-release lifecycle the skill previously ended without: triage with reproduce-first, the hotfix path (branch from the release tag, regression test that fails before and passes after, the full Phase 7 gate compressed in time but never in content), rollback (WordPress.org `Stable tag` downgrade, redeploy, deprecate — never unpublish), dependency and CVE duty ("Tested up to" is never bumped untested), the recurring-feature cycle, the full-suite rule, and the site-freshness duty. Resume routes here when PROGRESS.md marks Phase 7 done.
- **`references/security/website.md` + a routing row for websites** — the profile Phase 8 referenced six times but no rule ever loaded: transport and security headers verified with `curl -sI` on the live site, forms as the one dynamic surface (server-side validation, rate limit, honeypot + time-trap anti-spam, CAPTCHA only with an accessible alternative, SPF/DKIM/DMARC-aligned mail), zero mixed content with SRI on approved third parties, deploy integrity.
- **MCP profile: "Model-facing threats"** — the section that makes an MCP server different from a classic API: tool results carrying third-party content are data, never instructions (the injection channel); tool descriptions are prompts whose integrity is release-controlled (poisoning/rug-pull); confused deputy (per-user credentials, never a master token any caller can drive); destructive tools gated behind human confirmation or dry-run by default; localhost binding and Origin validation on local transports (DNS rebinding). Plus matching test points and a malicious-content fixture.
- **WordPress profile: five verified gaps closed** — SSRF via `wp_remote_*` with user-influenced URLs, object injection (`unserialize` across a trust boundary), identifier placeholders (`%i` / allow-lists for ORDER BY and column names), `hash_equals()` for tokens and license keys, and output-escaping of translated strings (a malicious `.po` is a real vector). The embedded MCP/OAuth section is now the WP mapping only and orders loading the full MCP profile.
- **Web-app profile: credential storage and recovery** — argon2id/bcrypt, MFA available and required as an option for privileged accounts, single-use time-boxed hashed reset tokens, no account enumeration, rate limits.
- **"Verify with" blocks in all five security profiles** — the checks now name their tools: `phpcs` with the WordPress security sniffs and the Plugin Check plugin; `npm audit`/`composer audit`/`pip-audit`; OWASP ZAP baseline; MCP Inspector with schema fuzzing; published-artifact inspection and public-API diff for libraries. At a test point, the command and its result are the evidence — an unrecorded check did not happen.
- **Four new project subagents in `references/claude-config.md`, and the existing three are now actually invoked.** New: `design-fidelity-auditor` (fresh-context fidelity walk of the build vs BUILD-SPEC and handoff), `playground-qa` (follows `docs/playground.md` literally — an instruction gap is a defect), `launch-verifier` (crawls the deployed site from the sitemap and returns the pass/fail table), `a11y-auditor` (automated accessibility pass + the guided-loop script). Wired: `code-reviewer` reviews every slice diff before its commit (Phase 5 test-point confirmation (f)), `docs-verifier` runs at sprint closes, `security-auditor` before the Phase 7 tag — previously defined but never referenced by any phase. Plus an optional CI workflow piece built from the technical plan's exact verified commands, with gitleaks and `scripts/keel-verify`.
- **`scripts/keel-verify`** — every project generates its own release linter at the Phase 5 scaffold: version touchpoints in sync, changelog order, `docs/api/INDEX.md` parity, no placeholder copy in distributable surfaces, and a WP i18n dry run where it applies. Runs at every sprint close and at the Phase 7 gate, output pasted as evidence.
- **Phase 5: the testing contract the user asked for — right the first time.** Every acceptance criterion maps to a named automated test (unit / integration / e2e; browser-driven e2e required for web UI flows) or carries a recorded one-line justification; every test-point row records the exact commands, output summary and commit hash ("a result without its command and output is an empty cell"); after three failed attempts at the same test point the assistant STOPS and reports instead of looping; sprint closes run the FULL suite, `docs-verifier`, a cold `playground-qa` pass and `keel-verify`; the playground is re-booted from its documented commands at each session's first test point and stamped `last verified`; declared performance budgets are measured, with numbers. Two new principles seal it: anything a compile, a boot or a basic test would have caught is a process defect if it escapes; and a failing test is never deleted, skipped or weakened to pass.
- **Phase 4: the audit leaves evidence.** BUILD-SPEC §1 becomes an evidence table mirroring every gate bullet (item / checked / evidence / result — "an item without evidence is not audited"); a deterministic mechanical pass checks the filesystem facts (assets exist, SVG+PNG pairs, screens covered, open questions empty); token origin and full target-surface coverage are now audited (they were contract terms nobody checked); declared contrast pairs are recomputed from the delivered hex values; re-deliveries are diffed against the previous delivery (byte-stable is verified, the BUILD-SPEC is updated, the second bounce of an item escalates to the user); checkboxes are ticked in the file at the moment of verification.
- **Fonts are first-class handoff assets, like logos and icons (handoff contract, rule 4).** Design ships the actual self-hosted font files at their final paths — only the used weights/styles, `woff2` first for the web, the `@font-face` rules already written against those paths — or, when licensing prevents shipping, a complete acquisition block per font: exact download source, license note, exact files, exact target path in the project structure, so the user drops them in and everything works and stays reusable. The Phase 4 audit checks it, the guided loop walks the acquisition one font at a time, and a font that is merely named is an incomplete handoff.
- **Phase 3: no more single point of failure on Design.** An "If Claude Design is not available" playbook with three recorded branches (round-trip through another environment, the assistant produces the handoff under the same contract and audits it with no self-exemption, or a human designer); a brief approval gate (mechanical scan for unfilled brackets + the user approves a summary); the exact waiting position lands in PROGRESS.md; and visual references travel as annotated SCREENSHOTS, never URLs — if the user offers an address, the assistant explains that captures of what they want are far more effective than expecting Design to visit links (field-tested).
- **Phase 2: the spec defends itself.** An adversarial fresh-context review before the firm estimate (six-part requirements, flow coverage, per-screen accessibility, acceptance-criterion completeness, empty/permission/double-submit ambiguities); the Testing block names frameworks and exact commands per layer, the playground recipe, seed data and the per-flow real-exercise command; the design split now carries foreseen external assets, per-screen accessibility requirements and exact target viewports/breakpoints all the way into the template (they previously died between the step and the brief).
- **Phase 1: the edges get playbooks.** Zero-competitors handling (retry differently, then record "no competitors found" as analysis: new niche or no market); "partial" scan status defined; a closure protocol for the honest assessment (verdict + user decision on record; `parked` status when the project stops; proceeding against a negative verdict is a recorded decision); the competitive scan delegates to subagents when available; and the `Client budget:` question, asked once.
- **Phase 8: launch becomes auditable, and the site outlives it.** `<site-docs>/launch-report.md` — every checklist item leaves a row (command/tool, result, date), with the page list derived from `sitemap.xml` and EVERY page checked (sampling one per template is called out as insufficient); a security headers and forms block verified live; the automated accessibility pass runs over the full sitemap list while the manual pass follows the guided loop; an accessibility-statement page is required in EU scope; the 404 page joins the section catalogue as a designed template; anti-spam is now defined (in the website profile) instead of referenced; and a new "After launch — operations" section produces `<site-docs>/operations.md` (renewal dates including security.txt `Expires`, uptime and backup decisions, Search Console submission, and the freshness duty every product release triggers).
- **Accessibility: the guided assistive-technology pass.** The assistant cannot run a screen reader, so the real-AT pass is a one-instruction-at-a-time user loop with per-item recorded results; what cannot be verified is `⚠ unverified` — and Phase 7 now closes every unverified item (re-attempt or explicit user acceptance on record). Automated tool passes are explicitly the assistant's to run, with commands and results recorded.
- **Project state: scope changes, deferred work, parked projects.** A "Scope changes" playbook (decision entry → spec amendment → design delta with re-audit → BUILD-SPEC delta → sprint re-plan → estimate/budget recompute — never smuggled through a Design Request); a "Deferred items" list with severity and review triggers (the greenfield counterpart of adoption's triage); `parked` as a recognized status; the `Client budget:` card line; the token-ledger row wired into the continuation-prompt procedure so mid-work sessions stop losing theirs; and the cache-discipline list corrected (issues, token ledger and playground are routinely-updated files).
- **MANIFEST: Table 1 completed and Table 3 added.** New parity rows the references already required but the "ABSOLUTE parity check" never listed: `docs/security.md`, `docs/accessibility.md`, the repo README, `docs/design/design-handoff/`, `scripts/keel-verify`, the optional CI workflow, and the Phase 8 site artifacts including `launch-report.md` and `operations.md`. Table 3 is the per-version action list — the reconciliation applies a delta instead of interpreting the changelog.
- **Every chat or tool boundary ships a ready-to-paste prompt (new operating principle).** Code to Design: the brief's complete opening message is assembled and shown, and the Phase 4 return prompt is produced at the same moment — days may pass before the handoff comes back, and the user must not have to ask for the way back in. Design to Code: the kickoff prompt. Chat to chat: the continuation prompt, shown unprompted. External agents (e.g. a competitive scan run elsewhere): the full prompt, composed by the assistant. Telling the user "ask Design to fix X" without the exact prompt is now a defect — the user is the courier, never the composer.
- **Plan mode, sprint kickoffs, and user verification per sprint (Phase 5).** The skill now says when to enter plan mode: at sprint-set creation (step 0) and at the start of EVERY sprint, where the kickoff also re-validates assumptions against the source — the current code, platform and dependency versions, external APIs/hooks/schemas — before building on them (a sprint built on a stale assumption is rework with extra steps). Every sprint closes with the full suite green and everything committed, then hands the user a numbered try-it script for exactly what the sprint implemented, points them at the debug log, and asks for the verdict either way; a reported failure reopens the sprint with its regression test. The continuation prompt is then SHOWN proactively with the instruction to paste it into a new chat — the user never has to ask for it.
- **Debug logging with a switch, built into the product itself (Phase 5 scaffold; decided in the Phase 2 plan; verified at the Phase 7 gate).** Every runnable project ships a copy-paste-friendly debug log so the user can hand the assistant a debuggable failure in one round trip: platform-native where one exists — WooCommerce's `WC_Logger` read under WooCommerce → Status → Logs, a settings checkbox in WP/PrestaShop/panel apps, an env var or constant for servers/CLIs/libraries, stderr for MCP servers (never stdout) — with entries that never contain secrets or real personal data. ON during development, OFF by default at release, and the logging code is never stripped: a production incident with a switchable log is diagnosable the same day.
- **End-user HTML guide — `guide/` at the repo root (Phase 6).** `docs/` serves developers; the end user gets a navigable HTML guide — a main index plus one page per topic — that is VERY detailed by contract: every capability the product has appears as a task with exact steps, every setting is explained, and the completeness check is mechanical (every v1 feature and every setting maps to its guide section — a gap blocks the phase). Two questions, asked with defaults: the language(s) — one directory per locale, English recommended as the principal when there are several — and whether the guide ships in the release package (yes → it is a distributable surface, placeholder-scanned and verified on the candidate; no → `/guide/` is export-ignored). Vanilla self-contained HTML that works from disk, accessible per the a11y reference, troubleshooting section wired to the debug-log switch. Once it exists, nothing is left dangling: a slice or maintenance change that adds or changes user-visible behavior updates the guide in the SAME change, in every locale; and on a Keel update of an existing project, the reconciliation itself asks whether to create the full guide now. Recorded on the project card (`User guide:`).
- **The skill's own test infrastructure** (repo-only, export-ignored): `tests/lint-release.py` — version sync across frontmatter/heading/changelog/manifest/README, description length within the 1024-character installer limit, changelog order oldest → newest, reference-index parity against disk, MANIFEST Table 2 parity against the tree, canonical lock-stamp freshness; `tests/evals/` — six documented eval scenarios (vague idea, resume, stale embedded copy, version-bump bait, synthetic secret, trigger boundaries); and a GitHub Actions workflow that runs the linter on every tag and publishes the release with notes extracted from this changelog.

### Changed
- **Frontmatter description rewritten under the 1024-character installer limit** (the 1.13.0 text had grown to 1,133 characters — a packaging regression its own history warns about twice), trading the feature-catalog sentence for a negative trigger boundary ("Do NOT trigger for one-off scripts, quick code questions, or repos not managed by Keel unless the user wants to adopt them") and the maintenance triggers (hotfix, dependency updates).
- **Phase 7: the project's version is proposed, never decided.** The assistant proposes patch/minor/major with reasoning and WAITS for explicit approval before writing any number — the same discipline that protects Keel's own version, now at project scale. The pre-release gate re-runs the entire suite on the exact release candidate (recorded), runs `keel-verify`, closes every `⚠ unverified` item, verifies zero placeholder copy and the performance budgets, and hands the project to `references/maintenance.md`.
- **Phase 8 vanilla rule made consistent:** the permitted exceptions are exactly the two the user approves and records — a static-site generator where it adds value, and the analytics decision from site discovery — replacing two contradictory "only exception" claims. A later explicit user request for the site supersedes a recorded Phase 1 "no", as a new decision entry.
- **robots.txt AI-crawler taxonomy corrected** into two axes: assistants and answer engines that fetch or cite for users (`Claude-User`, `Claude-SearchBot`, `PerplexityBot`, `Perplexity-User`, `OAI-SearchBot`, `ChatGPT-User`, `DuckAssistBot` — usually allow) versus training crawlers (`GPTBot`, `ClaudeBot`, `Google-Extended` — which governs Gemini training/grounding and does NOT affect Google AI Overviews — `Applebot-Extended`, `CCBot`, `meta-externalagent`, `Bytespider` — a recorded user decision, never a silent default). The volatility hedge now orders verifying each vendor's current crawler documentation at launch. The previous grouping misclassified `ClaudeBot` as a citing answer engine.
- **Technical SEO gains an interoperability note for dedicated SEO skills:** keyword/intent research and per-page briefs run BEFORE the section catalogue and feed the copy; this file remains the single writer of the technical artifacts.
- **Secrets carve-out across the whole design pipeline:** `SPEC/external-setup.md`, the Design Request register, the brief and `docs/BUILD-SPEC.md` record descriptive placeholders for anything that is a secret; the real value is exchanged only inside the Phase 4 guided walkthrough. The previous text ordered secrets recorded into versioned files — in direct collision with the UNBREAKABLE confidential-data rule and the project's own pre-commit gate.
- **Client budget is conditional:** asked once at Phase 1 ("is there a client to bill or a quote to produce?" — `Client budget:` card line); without a client, no `docs/budget.md` and none of the rate/currency/language questions. The estimate and the token ledger remain unconditional. MANIFEST Table 1 and SKILL.md's estimation section agree with the phases again.
- **`build-spec-template.md` produces `docs/BUILD-SPEC.md`** (the canonical path every other file already used), represents multi-surface token mappings, and its §1 is the evidence table.
- Phase 1 step 7's heading no longer calls the project website "a separate skill" (residual text from before the keel-web absorption; SKILL.md always said the opposite).
- Version-policy text says "four locations" in both places (a "three" survived the 1.12.0 addition of the manifest); SKILL.md's examples use placeholders instead of literal version strings; NOTICE no longer carries its own version line (it pointed at 1.0.0 for thirteen releases); the canonical lock block's stamp is kept equal to the released version as release hygiene, linter-checked.
- CHANGELOG history repaired: the initial feature set recorded under a duplicated "Added" heading in 1.1.0 now lives under 1.0.0 where it belongs.
- `.gitattributes`: duplicate `/.editorconfig` export-ignore removed; the tests directory is real now.
- **README and INSTALL refreshed and de-drifted:** version and phase table current (README had been a release behind for the third time); INSTALL's tree includes `MANIFEST.md`, its release-archive note describes the real tarball layout (a `keel-skill-vX.Y.Z/` root containing `keel/`), and installation now covers Claude Code, Cowork, the Claude apps (claude.ai zip upload) and the API, plus the skills marketplace.

## 2.1.0

### Added
- **The handoff directory is exclusively Design's delivery — wholesale-replaceable, always (handoff contract, new rule 10; field-tested).** Nothing but the delivered bytes ever lives in `docs/design/design-handoff/`: no generated assets, no user-acquired fonts, no notes or audit evidence, no build output. The reason is operational: installing a delivery or a re-delivery is a whole-directory swap — archive the current delivery to `docs/old/design-handoff/<DR-id-or-date>/` (move, never delete), place the new one, diff against the archived copy — and a single foreign file makes that swap unsafe and blocks it, which had repeatedly happened in the field. Phase 4 gains the matching non-negotiable rule (its seventh), its Step 1 mechanical pass now flags any file the delivery's own SPEC does not account for, and Step 6 targets the PROJECT's asset tree for user-generated assets and acquired fonts (the font acquisition block's target path is now explicitly a project path, with the delivered `@font-face` referencing that final path). Phase 3 states the rule at the exact moment the returned handoff is placed.

## 3.0.0

Major release: **Keel becomes multi-assistant.** Everything the workflow used to materialize only for Claude — the portability lock, the embedded skill, the native config package — now has a first-class container for every assistant the user actually works with: OpenAI Codex, GitHub Copilot, Cursor, Gemini CLI and Windsurf alongside Claude Code, riding the two open standards that consolidated in 2025-2026 (AGENTS.md for agent instructions, Agent Skills for the skill format itself) so most other tools — opencode, Zed, Warp, JetBrains Junie, Kiro, Cline and more — are covered by the standards at no extra cost. The principle throughout: one source of content (the project's own recorded decisions), one container per tool, parity between containers as a duty.

### Added
- **The portability lock is dual, always (`references/project-state.md`).** The same KEEL block now lives in `CLAUDE.md` AND `AGENTS.md` in every project — created together, refreshed together, stamped together. Claude reads the first; Codex, Copilot, Cursor, Windsurf, opencode, Zed, Warp, Junie, Kiro, Cline and most other tools read the second natively. Gemini CLI, whose default context file is `GEMINI.md`, gets one recorded pick when the user works with it: a `GEMINI.md` mirror, or `context.fileName` in `.gemini/settings.json` including `AGENTS.md`. The lock text itself is now assistant-neutral (embedded-copy paths name both trees; the continuation-prompt pointer no longer assumes `.claude/`). The lock-freshness check (`references/keel-maintenance.md`) refreshes both files — and creates the missing `AGENTS.md` on projects that predate the dual lock.
- **The embedded skill is dual (`references/project-state.md`).** The embed now goes to `.claude/skills/keel/` + `.agents/skills/keel/`, identical byte for byte: between the native tree and the open Agent Skills discovery convention, virtually every assistant loads Keel as a project skill on its own. The verified full-copy protocol applies to each tree; version sync always touches both, so they can never diverge. The update check (`references/keel-maintenance.md`) compares and updates both trees, and user-level install locations now include `~/.agents/skills/keel/` alongside `~/.claude/skills/keel/`.
- **`references/assistant-config.md`** — replaces `references/claude-config.md`, generalized. The offer now asks which assistants will work on the repo; the package materializes one container per accepted tool from the SAME sources (technical plan §Conventions, the loaded security profile, Keel's quality gates, the verified commands): rules with path scoping (`.claude/rules/` with `paths:`, `.cursor/rules/*.mdc` with `globs:`, `.github/instructions/*.instructions.md` with `applyTo:`, `.windsurf/rules/*.md` with `trigger: glob`, and nested context files per source directory for Codex/Gemini, which have no glob mechanism); the seven verifier subagents (`.claude/agents/` — which Cursor also reads natively — `.github/agents/*.agent.md`, `.gemini/agents/*.md`; Codex and Windsurf run the same checks inline per the standing fallback); permission allow-lists (`.claude/settings.json`, `.codex/rules/` Starlark prefix rules, `tools.allowed` in `.gemini/settings.json`, `.cursor/cli.json`; Copilot and Windsurf have no committable equivalent — the verified commands are documented in the repo's development notes instead); and MCP registration (`.mcp.json`, `.cursor/mcp.json`, `.vscode/mcp.json`, `[mcp_servers]` in `.codex/config.toml`, `mcpServers` in `.gemini/settings.json`). New standing rules: a "—" cell removes the mechanism, never the duty; parity across containers is verified at the Phase 7 gate; per-tool size limits are respected by construction (Keel's under-40-lines rule sits far inside all of them); config syntax rots — the content is Keel's contract, the container is the tool's, verified against current docs when in doubt.
- **Project card line renamed: `Assistant config: [none / rules / rules+agents / full] (tools: ...)`** — replaces `Claude config:`, now recording WHICH tools were accepted. MANIFEST Table 1 rows updated accordingly (lock, embed, rules, subagents, permissions, MCP registration all name their per-tool containers), and Table 3 carries the v3.0.0 reconciliation delta for existing projects (rename the card line, create the missing `AGENTS.md`, add the second embed tree, offer the new tools' containers, extend `.gitignore` and the gate exemption).
- **INSTALL.md: per-assistant installation.** A user-level install-path table (Claude, Codex, Cursor, Gemini CLI, Copilot/VS Code, Windsurf, and the shared `~/.agents/skills/` convention that serves the standard-compliant tools with one copy), plus the note that an embedded Keel project needs no per-machine install at all. README and INSTALL are now written for the multi-assistant reality (repo docs).

### Changed
- **The confidential-data pre-commit gate exempts both embedded-skill trees** (`.claude/skills/*` and `.agents/skills/*` — the canonical trees that legitimately contain the gate's own patterns); the assistant-side hook note now points at the native pre-tool-use hooks of Claude Code, Codex, Gemini CLI and Cursor as the optional layer on top, git hook always the baseline. The scaffold's `.gitignore` adds the accepted tools' personal files (`AGENTS.override.md`, `.gemini/.env`, `.gemini/tmp/`) to the unconditional trio.
- **Phase 7 export-ignore covers every generated assistant config tree** (`.claude/`, `.agents/`, `.codex/`, `.cursor/`, `.gemini/`, `.windsurf/`, `.github/instructions/`, `.github/agents/`, `.vscode/mcp.json`, nested context files, `GEMINI.md` when kept) — nothing from the package ships, whatever the tool. The pre-tag verification checks container parity, not just the Claude tree.
- **Phase 5 plan mode names its equivalents** (Claude Code, Cursor, Gemini CLI's plan approval mode) with the same no-plan-mode fallback; the scaffold completes the package per tool; the subagent hooks in Phases 4/5/7/8 and the accessibility reference now point at `references/assistant-config.md`.
- **`references/estimation-budget.md`:** subscription mode names the other flat-fee plans (ChatGPT Plus/Pro, Gemini AI plans), the price-verification step carries the OpenAI and Google official pricing pages next to Anthropic's, and the measured-usage examples add Codex `/status` and Gemini CLI `/stats` beside Claude Code `/cost`.
- **SKILL.md:** the portability paragraph, the shared-templates entry and the reference index describe the dual lock, the dual embed and the generalized package; the token-economy line no longer assumes the coding assistant is Claude Code.

## 3.1.0

The verification loop closes over the end-user guide, and code comments become a reviewed contract.

### Added
- **`guide-qa` — the eighth verifier subagent (`references/assistant-config.md`).** Conditional like its siblings: generated at Phase 6 on first need, once the end-user guide decision is yes (the decision does not exist earlier). Fresh context, read-only: it receives ONLY `guide/` plus the capability and settings lists, and verifies mechanical coverage (every capability and every setting has its guide section, troubleshooting covers the debug-log switch), that every internal link and image resolves (the offline rule), that every task's steps are followable exactly as written, per-locale orthography, and the guide's own accessibility basics. The session that wrote the guide no longer self-certifies it: Phase 6 runs `guide-qa` at the guide check, and the Phase 7 gate re-runs it on the exact release candidate when the guide ships in the package — inline with the standing fallback when the environment has no subagents.
- **Comments are a reviewed contract.** The `code-style` rule gains its comments line and `code-reviewer` gains check (7): every public surface in the diff carries its docblock per the platform's convention (purpose, params, return), non-obvious decisions carry a why comment, and comment language follows the recorded policy (English by default). Phase 5's slice loop writes them at creation — "Comment the code as you write it", never backfilled later — exactly like docs and accessibility.

### Changed
- **Phase 6 definition of done:** the `docs/api/INDEX.md` one-to-one check is delegated to `docs-verifier` when the environment provides subagents (inline otherwise), and the guide's mechanical coverage check names `guide-qa` as its runner.
- **MANIFEST Table 3** carries the v3.1.0 reconciliation delta: on projects with the assistant config package accepted, regenerate `code-reviewer` and the `code-style` rule in every capable container; where the `User guide:` card line is yes and `guide/` exists, generate `guide-qa` at the next Phase 6 or maintenance touch.

## 3.2.0

The guide gets its canonical face: `guide/` is now built on **keel-docs-theme**, the versioned documentation theme designed once (art direction "Errata") and applied to every project — never improvised per-project HTML/CSS again.

### Added
- **`references/guide-theme.md`** — the complete consumption mechanism for the canonical theme (public repo `joseconti/keel-docs-theme`, semver releases publishing a version-stamped zip + SHA-256 `checksums.txt`): obtain the latest release (user-courier branch when the environment has no network), verify integrity BEFORE vendoring, vendor `_theme/` as an untouchable unit (project-side needs go upstream as theme issues, never local forks or edits), fill the brand layer recomputing the contrast contract — never trusting it — (the José Conti preset is asked once, reused thereafter, and offered upstream), generate the pages from the slot templates, render the developer portal from `docs/` (which remains the single source of truth; coverage one-to-one vs `docs/api/INDEX.md`), generate the per-project data files (search index + locale strings; the 32 keys are the stable contract), and register the version on the record (`Docs theme:` card line + per-page `<meta name="keel-docs-theme">`). Updates are offered, never imposed, never silent, never partial.
- **Phase 6 asks four guide questions, not two:** languages and ships-in-release as before, plus the developer portal (recommended yes for projects with a public API/hook surface) and its own ships/repo-only choice. The guide build rules replace hand-rolled "vanilla, self-contained, offline" HTML with "built on the canonical theme — offline by inheritance": never per-project CSS, never an edited `_theme/`.
- **`guide-qa` gains check (6) — the theme checks:** every page's `keel-docs-theme` version meta equals the `Docs theme:` card line; `guide/_theme/` matches the theme release's `checksums.txt` (SHA-256 recomputed); dev-portal coverage vs `docs/api/INDEX.md` one-to-one when the portal exists. Its inputs now include the card line, the checksums file and the API index; the Phase 7 gate enumerates the theme checks on the exact release candidate.
- **Maintenance gains the docs-theme freshness duty:** check whether a newer theme release exists and OFFER the update — what it brings (theme changelog) and what it means (wholesale re-vendor of `_theme/`, checksum re-verification, `guide-qa` re-run). A decline is recorded and re-offered at the next pass, never nagged mid-work.
- **The project card gains the `Docs theme:` line;** the `User guide:` line now also records the developer-portal choice.

### Changed
- **MANIFEST Table 1** adds the vendored-theme row (`guide/_theme/` + `guide/brand/` + per-page version meta, unless the guide was declined); **Table 3** carries the v3.2.0 reconciliation delta: add the `Docs theme:` card line; where a pre-theme `guide/` exists, offer rebuilding it on the canonical theme at the next Phase 6 or maintenance touch (asking the never-asked developer-portal questions); regenerate `guide-qa` in every capable container where it exists.

## 3.2.1

Release-asset verification closes the loop the canonical theme opened — found in the field on keel-docs-theme v1.0.0's very first consumer: the release body DESCRIBED its assets, none were actually attached, and the publishing session had verified the rendered page instead of the attachments.

### Added
- **Phase 7: a forge release that publishes downloadable assets is verified AFTER publishing, never assumed.** `gh release view <tag>` or the forge API's assets list, confirming each expected asset's name AND size — never the rendered release page (a body that describes assets is not an attachment). When the environment has no network or no `gh`, the user runs the check from their terminal and reports back. The verification (names + sizes) is recorded in `docs/07-release.md`; missing assets mean the release is NOT shipped — upload (`gh release upload`) and re-verify. Added to the phase's definition of done.
- **`guide-theme.md`: the empty-assets branch warns the maintainer.** When the theme's latest release lists no downloadable assets (checked via `gh`/the API, not the page), the clone-at-tag + `scripts/package.sh` fallback is used — it produces the identical stamped package — AND the user is told explicitly that the theme release is incomplete so it gets fixed upstream. The fallback never becomes the silent norm.

## 3.3.0

Per-role model binding: Keel stops running every agent on the session model and binds each one to the model that fits its work — chosen once per project, in the assistant's own config, never named in the skill. The lever usage statistics kept pointing at (subagent-heavy sessions all inheriting one expensive model) closed without breaking multi-assistant portability.

### Added
- **`references/assistant-config.md` gains "Model binding — abstract roles, concrete models per project".** Every agent is assigned an abstract ROLE by the kind of work it does — `orchestrator` (the session driver and the judgment-heavy phases), `reviewer` (`code-reviewer`, `security-auditor`, `design-fidelity-auditor`), `mechanical` (`docs-verifier`, `playground-qa`, `launch-verifier`, `a11y-auditor`, `guide-qa`, plus any search/exploration agent) — and the concrete model behind each role is chosen per project. The skill never names a model: the binding materializes into each tool's native model field (`model:` in `.claude/agents/*.md`, an `[agents.<name>]` entry in `.codex/config.toml`, `model:` in `.github/agents/*.agent.md` and `.gemini/agents/*.md`), using each vendor's own identifiers — so a model name that ages never reaches a file that travels. Recommend, don't interrogate: at the config-package offer, when agents are taken, propose a role→model map with a one-line reason each and let the user confirm or override; the sensible default (strongest → orchestrator, mid → reviewer, cheapest → mechanical) is applied and recorded if the user does not engage. The cost rationale is stated honestly — on a flat subscription a cheaper model buys speed and rate-limit headroom, not money; on metered billing it is direct spend. Persisted in three places, each with its job: a `docs/decisions.md` D-entry (the reasoning), the project card `Models:` line (read on every resume), and the tool's native config (the only place a concrete model name lives). Re-confirmed only on the user's request or when a configured model errors as unavailable — never re-asked every session.
- **The project card gains the `Models:` line** (`references/project-state.md`): the role→model map per accepted tool, or `n/a` when no agents are configured.

### Changed
- **The subagents section's model rule is inverted.** Where it read "do not pin a `model:` — inherit the session's" — which quietly ran the read-only mechanical verifiers on whatever expensive model happened to drive the session — it now assigns the model by ROLE and forbids a model name in the skill or the lock, taking the binding from the project's recorded map. The pieces-and-timing table and the container matrix each gain a model-binding row (materialized at Phase 2 close, alongside the subagents); the offer settles the map in the same conversation.
- **SKILL.md** names per-role model binding in the `references/assistant-config.md` description line.
- **MANIFEST Table 1** lists the `Models:` card line; **Table 3** carries the v3.3.0 reconciliation delta: add the card line, and where the assistant config package includes agents, settle the map and materialize the native model fields at the next Phase 2-close or maintenance touch.

## 3.4.0

Build assets are source-first: production ships minified files, but the unminified source always exists alongside as the single source of truth, and the minified file is only ever generated from it — never hand-edited. Minification runs locally as the build step the working assistant executes, never delegated to CI or a forge action. Added as a standing, developer-overridable contract.

### Added
- **`SKILL.md` — new cross-cutting contract "Build assets — source first, minified for production", plus its operating-principle bullet.** Every project that ships front-end assets loads the minified form (`*.min.js`, minified CSS) in production, but the unminified source always exists beside it as the single source of truth. The flow is one-directional — edit the source, regenerate the minified file — and never reversed; a `.min.*` is never created or edited by hand (a desync defect). Minification runs locally as the build step the working assistant executes (Claude Code, Codex, Copilot, Cursor, Gemini CLI, Windsurf), never delegated to CI or a forge action, and a project never assumes a remote pipeline produces its minified assets. Pairing convention (`name.js` + `name.min.js`, `name.css` + `name.min.css`, same directory) doubles as WordPress `SCRIPT_DEBUG` support. Overridable only by an explicit `docs/decisions.md` decision; projects with no front-end assets never trigger it.
- **Phase 2 (`references/phase-2-functional-spec.md`):** the technical plan names the front-end build/minify script and the source→minified pairing convention when the project ships JS/CSS; Definition of done extended.
- **Phase 5 (`references/phase-5-development.md`):** the build/minify script is created at the scaffold with the source→minified pairing from the first asset; a per-slice rule regenerates minified assets from source (never hand-edited); `scripts/keel-verify` gains a minified-asset-sync check.
- **Phase 7 (`references/phase-7-release.md`):** the release gate regenerates every minified asset from its source on the candidate and verifies each pair is in sync — a stale or hand-edited minified file blocks the release; Definition of done extended.

### Changed
- **`MANIFEST.md`:** Table 1 gains the conditional build/minify script row; Table 2 re-read map updated for the changed files; Table 3 adds the v3.4.0 reconciliation action for existing projects that ship front-end assets.

## 3.5.0

Documentation now follows the code through all three operations, not only creation. Every rule in the skill was written for *new* public surfaces, which left two silent holes: a changed signature could keep its old doc indefinitely, and a removed surface could leave its entry behind — both indistinguishable, to any later reader, from documentation that is simply correct. This release closes them and, alongside, lands the two context-engineering additions authored on 24 July: a requirement may be carried by a reference artifact instead of prose, and one domain of quality may be governed by a recorded rubric.

### Changed
- **`SKILL.md` — the documentation principle now covers created, modified and removed surfaces.** Created: full entry (signature, params, return, errors, auth, runnable example) plus its `docs/api/INDEX.md` row. Modified: the existing entry is updated in the same slice — signature, parameters, return, errors, permissions, behavior, and the example, re-run — because a doc describing the previous signature is worse than a missing one: the reader has no way to know it is stale. Removed: never a silent deletion — entry and row are deleted if the surface was never released; if it was, removal is a gated breaking change and the entry stays, marked deprecated/removed with its version and replacement, plus a D-entry in `docs/decisions.md`. The slice does not pass its test point until the docs match the as-built code and every touched example runs.
- **Phase 5 (`references/phase-5-development.md`):** the principle, the per-slice documentation rule, test-point check (b) and the Definition of done all restated across the three operations. The slice also updates whatever else referenced the surface — other docs' examples, the technical plan's code map, `docs/architecture.md`.
- **Phase 6 (`references/phase-6-documentation.md`):** one-to-one INDEX parity is declared necessary but not sufficient — no entry may describe a surface the code no longer has, or a signature it no longer matches. A surface removed after release survives only as a deliberate deprecated/removed entry naming its replacement.
- **`references/project-state.md`:** `docs/api/INDEX.md` is updated in the same slice that adds, changes **or removes** a surface; a row pointing at a symbol the code no longer has is a defect like any other.
- **`references/assistant-config.md`:** `docs-verifier` now checks all three operations in the diff instead of additions only — a changed signature whose doc was not updated is a finding, and so is a doc or row describing a symbol that no longer exists (unless it is a deliberate deprecation). `code-reviewer` check (5) and the generated build-discipline rule restated to match.

### Added
- **Phase 2 (`references/phase-2-functional-spec.md`) — reference artifacts.** A requirement may be carried by a file rather than paraphrased into prose: a test suite as executable spec, code to port, a working HTML mockup. Four conditions, none optional — it lives in the repo (`docs/spec-references/`, or `docs/design/references/` for visual references), a tests-as-spec suite is wired into the project's real test command, ported code carries verified provenance and license compatibility (incompatible or unknown → not ported, re-specified and written fresh), and each artifact is registered in the spec's new `## Reference artifacts` section. The prose requirement stays, reduced to what the file cannot say about itself; where artifact and spec disagree, the spec governs.
- **Phase 2 §6a — domain rubrics.** After the mechanical adversarial review, the user is asked once, in plain language, whether to record what "good" means for one domain the checklist cannot tick — API ergonomics, visual coherence, voice. Recommended for plugins, MCP servers and libraries, where the shape third parties hook into cannot be changed after release without breaking their sites. A rubric lives in `docs/rubrics/<domain>.md` and is applied by the reviewer at authoring time under three rules: recorded never improvised (no rubric on record means no rubric pass), the reviewer flags but never rewrites, and the fixed artifact wins any disagreement. Asking and recording the answer is the requirement; taking a rubric is not.
- **Phase 3 (`references/phase-3-design-handoff.md`) — the fidelity ladder gains a rung.** A working HTML/CSS mockup outranks a screenshot, which outranks a URL: it carries structure, spacing, states and behavior in a language Design reads exactly. Rich references travel as the FILE, shipped with the brief materials with a one-line note on what to take and what to ignore — never paraphrased. Two limits: a mockup is a reference, never the delivery (it never enters `docs/design/design-handoff/`, per contract rule 10), and it does not shrink the brief. `references/design-brief-template.md` restated to match.
- **Phase 4 (`references/phase-4-faithful-build.md`) — principle 8:** a Phase 2 reference mockup is input to Design only, never a build source. Where it disagrees with `docs/BUILD-SPEC.md` or the delivery it is ignored; if it looks *more* right, that is a Design Request, not a reason to take its markup.
- **Phase 7 (`references/phase-7-release.md`):** the license gate covers ported code — its license compatible, its attribution present where the license requires it.
- **Phase 1 (`references/phase-1-discovery.md`):** the competitive scan is stated as first step of a NEW project, with adoption's recommended-but-optional treatment made explicit, removing a standing contradiction with `references/adoption.md`.
- **`MANIFEST.md`:** Table 1 gains the conditional `docs/spec-references/`, `docs/rubrics/` and `docs/design/references/` rows; Table 2 re-read map updated; Table 3 adds the v3.5.0 reconciliation actions.

## 4.0.0

Major release: **documentation and reality stop drifting by discipline and start drifting by failing a check.** Keel already demanded evidence over assertion; this version attacks the failure class that survives that demand — a doc, a map, a manifest or a security claim that quietly stops matching the code while both keep looking healthy. The response is never "write it more clearly": prose asks for discipline once per reader, a check enforces itself forever. So the code map now says which of its paths actually exist, every project carries the checklist of what each type of change must touch, the known traps of its class are read before starting instead of rediscovered, deliberate omissions are recorded rather than silent, and six new mechanical checks catch the rest.

### Added

- **`references/anti-patterns.md`** — the known-traps catalogue, loaded at Phase 1 the moment the project type is fixed, alongside the security profile and the accessibility reference. Twenty-three entries in the shape *the trap / why it happens / what it costs / the rule that closes it*: twelve universal (declared tools that never run, documentation contradicting the build, a fact pinned in five places, the target tree read as an inventory, the extension point nobody can reach, the generated artifact nobody consumes, verification claimed but not run, the gate loosened to get green, the silent omission, the control claimed but not shipped, two copies of one source of truth, the orphan document) plus sections for WordPress/WooCommerce, MCP servers, and web apps/websites/libraries. Every rule names a mechanical check wherever one is possible, because **a trap whose rule is only prose is a trap that is still open**. It closes with an eighteen-question self-audit where every answer must come from a command or an artifact, never from recollection — run at sprint closes, at the Phase 7 gate and at adoption, and the source of new `scripts/keel-verify` checks. It is the counterpart of `docs/lessons-learned.md`, not a replacement: lessons-learned is memory (what bit THIS project), anti-patterns is prevention (what has bitten this class of project before it started), and `references/project-state.md` now states which destination a finding goes to.
- **Code-map state markers `[E]` / `[A]` / `[G]` (`references/phase-2-functional-spec.md` §4).** Every row of the code map declares whether the path exists now, is to be created by the assistant in a named slice, or is generated by a tool once its source exists. The binding rule: **a path not marked `[E]` is absent until a slice creates it**, and no session may claim a path exists — or report work as in place — because the map shows it. Markers move in the commit that changes reality, exactly like the docs, and `keel-verify` checks that every `[E]` is really on disk. A tree diagram and a directory listing look identical on the page; this is what tells them apart.
- **The change map (`references/phase-2-functional-spec.md` §4b).** One row per recurring type of change in the project, naming every artifact that must be touched when it happens — for a WordPress plugin: a new setting touches registration, sanitize callback, default, uninstall cleanup, `docs/reference/`, the guide and the changelog. Keel already carried the rules; this turns them into a checklist specific to the project so no slice re-derives them, and it is the artifact that prevents the characteristic bug of any project with a public surface, a translation layer, a persistence layer and user docs. Read at the start of every slice, satisfied item by item at its test point, extended in the slice that discovers a new change type, and verified against the diff at the Phase 7 gate. On an adopted project it is often the most valuable artifact produced, because that knowledge lives only in the maintainer's head.
- **`docs/threat-model.md` (`references/phase-2-functional-spec.md` §4c) — including what is deliberately NOT defended.** Three sections: assumptions; the defended controls, each carrying a delivery state (`IN PLACE` built and evidenced, `TO BUILD` a named slice will build it, `MANUAL` a human must configure it, `VERIFY` only a real environment confirms it); and the **"Not defended"** table naming each deliberate omission with its consequence and what the user would add if their risk profile needs it. Only `IN PLACE` may be written in the present tense anywhere in the project's documentation. All five security profiles gained a **"Deliberate omissions"** section that seeds this table for their project type.
- **Seven new `scripts/keel-verify` checks (`references/phase-5-development.md` §1a).** Every command cited in `CLAUDE.md`, `AGENTS.md`, the README or `docs/` resolves to a real script or CI step (the documentation cites the script; the script is the truth). Internal doc links resolve and no document is orphaned. Every `[E]` code-map marker exists on disk. Code fences are balanced — an unclosed one silently truncates the document everywhere it is actually read. Every documented hook, filter, action or event name is found in the source, so an extension point that was renamed or never fired stops being invisible from inside the project. And the suppression count is reported and compared against the previous sprint close — growth is not an automatic failure, it is a number that has to be justified.
- **Phase 1 §3a — the competitive confrontation, where the scope now actually closes (`references/phase-1-discovery.md`).** The scan produced the category's baseline and step 3 produced a proposed v1; until this version nothing put the two lists side by side, which is how a v1 ships missing something every competitor has had for years — not because anyone decided against it, but because nobody ever compared. The confrontation presents **every** competitor functionality and external demand, never a curated subset, one row each carrying who has it, its cited demand evidence (or the honest "none found"), a rough per-feature cost and the assistant's recommendation. Then the user is asked **how** they want to decide: add everything (offered with the summed cost, so "everything" is a number and not a mood), add a selection, or — recommended — go through them one by one, deciding **in v1 / Later / never** with the cost visible at the moment of choosing. Every row ends in a recorded decision, **including the rejections**: a "never" becomes a `docs/decisions.md` entry so the same feature is not re-proposed and re-analysed six months later. Where the confrontation moves the scope materially, the honest assessment is revisited before the phase closes. The per-feature numbers feed the preliminary estimate instead of being derived twice.
- **"Every time figure is AI development time" is now an unbreakable SKILL.md principle in its own right.** Keel already based budgets on AI time; this raises it from a budgeting rule to a rule about **every duration the assistant ever states** — an estimate, a per-feature cost, a sprint projection, an offhand answer to "how long would that take?", a line in `docs/`. The unit is always the AI's working hours plus the vibe coder's supervision hours, and it is named every time, because an unqualified number is read as the human-team one and the gap between the two is orders of magnitude rather than a margin. A row that reads "3 weeks" when the truth is "4 hours of AI time plus 1 of yours" does not merely exaggerate — it inverts the decision, and the user drops a feature that was cheap. A human-team comparison is allowed only when explicitly labelled and placed beside the AI figure, never instead of it.
- **"When to stop and ask" (SKILL.md).** Eight rows in one table — the spec silent on a product decision, two artifacts contradicting each other, a dependency of consequence, a change that would contradict a recorded decision, three identical test-point failures, production data or money, anything that looks like a security problem, a required input that does not exist. Stopping is a successful outcome in every one; the failure is a plausible guess presented as completed work.

### Changed

- **Three new operating principles in SKILL.md.** *Documentation and reality never drift — and where they could, make the check fail.* *Declared is not delivered* — a control, a check or an artifact is only described in the present tense once it is true and evidenced; everything else carries its real state, and a check whose inputs do not exist yet is **"not yet applicable", naming what is missing** — neither passed nor failed. *A deliberate omission is recorded, always* — an omission written down is a decision, an omission that is silent is a trap.
- **`references/phase-5-development.md` gains §1b and §1c.** §1b makes "not yet applicable" a legitimate, named verification outcome and forbids using a later gate as a proxy for a missing earlier artifact — the early slices no longer force a choice between a false green and a false red. §1c defines the ONLY sanctioned escape hatch when the gate itself is wrong: fix the code → fix the gate in its own commit with the reason → suppress narrowly with a comment and a record → never an override, a deleted test, a relaxed assertion or a `--no-verify` commit. Both steps are recorded, and suppressions are counted.
- **Every slice now opens by confirming its inputs and reading its change-map row** (`references/phase-5-development.md` §2), and its test point gains two conditions: the change-map row satisfied item by item with any `[A]`/`[G]` path it made real now marked `[E]`, and any threat-model control it touched moved to its new delivery state (`TO BUILD` → `IN PLACE` only with the evidence).
- **The Phase 7 real-environment gate now walks the whole lifecycle** for anything that installs into a host platform — install → configure → uninstall → reinstall on a clean environment — verifying that uninstall removes every option, table, meta key, transient and scheduled event, and that the reinstall starts genuinely clean. It also exercises the support matrix at BOTH ends, floor and ceiling, so declared compatibility is tested rather than asserted. For a WordPress or WooCommerce plugin these are the two hardest support cases to reproduce, and they are only ever caught here.
- **The Phase 7 gate runs the self-audit** with every answer evidenced by a command or an artifact, verifies `docs/threat-model.md` is true of the release being tagged (shipping documentation that claims a protection the release does not have is a blocker, not documentation debt), and checks the code map and change map against the release diff. `docs/07-release.md` gains the two corresponding sections.
- **`docs/lessons-learned.md` restructured to symptom → cause → fix** (`references/project-state.md`), because a future session arrives holding a symptom, not a diagnosis, and an entry organized any other way is not found at the moment it would have helped. A **"Check added"** field makes closing a lesson include adding the check that catches it. Existing entries are not rewritten.
- **The portability lock (`references/project-state.md`) carries the new contract** in its step 3, so any assistant in any tool is bound by it even without the skill installed: the code map is a target tree, present tense requires evidence, "not yet applicable" is the honest answer for a check that cannot run, and the change map is read before changing anything.
- **Adoption (`references/adoption.md`) runs the self-audit against the real tree** as part of the gap audit and produces `docs/threat-model.md` at the project's honest delivery state — an adopted project usually surfaces several silent traps and more `TO BUILD` than expected, which is exactly the point. Its reconstructed code map is marked, and its change map is built from the change types the codebase actually has.
- **Phase 1 (`references/phase-1-discovery.md`) loads `references/anti-patterns.md`** as the third file the project type selects, recorded on the discovery template and in the definition of done.

### Removed

- The root `RELEASE-NOTES-vX.Y.Z.md` files. The release workflow builds the GitHub release body from the `CHANGELOG.md` section for the tag, so these duplicated the changelog and nothing referenced them — two copies of one source of truth, which is trap 11 of the very catalogue this release adds.

## 5.0.0

Major release, two ideas that share one root: **stop asking a human to do what a machine does better, and stop trusting anyone's memory for what a list can enforce.**

The first idea is testing. Keel already demanded real functional verification and a runnable playground — and then, sprint after sprint, closed by handing the user a numbered script of screens to visit and messages to confirm. That is the assistant outsourcing its own work: it is slow, it is not reproducible, it leaves no artifact, and it silently skips the empty, invalid and permission-denied cases where the bugs actually live. From this version the rule is explicit and unbreakable: **the assistant drives every test it can drive** — filling every field, walking every branch, asserting what the interface really shows, reading back what the machine saw — and the user is asked to touch the product only for what is physically impossible to automate, with the reason recorded from a closed list of eight.

The second idea is application. "Apply Keel to this repo" and "bring this project up to the new version" both failed the same way: a subset got applied, the rest quietly did not, and the omission surfaced only when the user asked why something was missing. The answer is not more diligence — it is a sweep read off `MANIFEST.md` row by row, landing in a project artifact where every applicable requirement carries a state, with a mechanical check behind it. An assistant cannot forget a row it is reading off a table.

### Added

- **`references/test-automation.md`** — the protocol that keeps the user out of the test-runner seat. It defines: the division of labour (what the assistant always drives, and the eight tags — `CREDENTIAL`, `HARDWARE`, `ASSISTIVE-TECH`, `JUDGMENT`, `EXTERNAL-APPROVAL`, `PLATFORM-IMPOSSIBLE`, `PRODUCTION-RISK`, `NO-EXECUTION` — that are the only valid reasons to delegate, with the anti-escape rule that anything else means automating it instead, and the scope rule that a credential blocks one leg and never the whole flow); making the product drivable (stable test identifiers on every interactive element, and the prohibition on faking an accessibility label to make a test pass, since that label is read aloud to real users); the `scripts/keel-doctor` contract; the driver per surface with its screen-stealing verdict; how forms are actually filled, in three shapes each — valid, empty, invalid; the ban on arbitrary sleeps; the read-back duty; the static analysers; accessibility as part of driving rather than a separate errand; the evidence rules; and an honest list of what genuinely cannot be driven.
- **`scripts/keel-doctor`** (`references/phase-2-functional-spec.md` §4d, generated at the Phase 5 scaffold) — the environment doctor, compiled from the technical plan's new `## Environment requirements` table rather than improvised. Three modes: `--check` detects only and never needs privileges, `--plan` prints the exact commands it *would* run without running them, `--fix` runs them after the user's OK on an itemized list they can decline row by row. Four states, not two — `OK`, `TOO OLD`, `NOT OPERATIONAL`, `MISSING` — because "installed but not running" and "absent" are different problems and collapsing them is what makes an assistant propose reinstalling something that only needed starting. Plus `--json` so the assistant reads structured output instead of parsing its own table. Nothing global is installed silently, no licence is accepted on the user's behalf, and what genuinely needs a human is reported as a clear stop instead of attempted.
- **Phase 1 §5a — the environment preflight.** The moment the project type and target platforms are fixed, four questions get answered before anything is planned around them: what must exist on this machine, whether any target platform is impossible here, whether testing will take over the user's screen and on which platforms, and what will need installing. Recorded in `docs/01-discovery.md` under `## Environment & test drivers`. A platform this machine cannot build or test changes the plan, not the schedule — and it is the user's decision, made in minute one rather than discovered in sprint three.
- **`test-driver` subagent** (`references/assistant-config.md`) — drives the interfaces and returns the evidence, with an explicit instruction never to report a criterion as passing because a human said so, and never to propose that the user walk a flow it could have driven itself. Bound to the **mechanical** role, because testing splits across two jobs that were previously conflated: deciding what to test is authoring and wants the strongest model, while executing the tests and triaging their failures is command-running against expectations already written down. Test runs are the most repeated agent invocation in Phase 5 — every slice, every sprint close, every session's freshness check — so this is the binding where a cheaper model pays back most often.
- **`docs/keel-conformance.md`** — the conformance sweep's artifact: one row per applicable `MANIFEST.md` Table 1 requirement, each carrying exactly one state (`present`, `missing`, `declined` with its `docs/decisions.md` entry, `n/a` with the condition that excludes it). A row with no state is an unfinished sweep. Created at Phase 1 step 0a, mandatory at adoption step 4a and at every post-update reconciliation, verified at the Phase 7 gate.
- **SKILL.md — two new cross-cutting sections and two new operating principles.** "Test automation — the assistant drives, the user decides" states the split that governs everything: the user decides what gets built, the assistant proves it works. "Applying Keel completely" defines the six-step sweep and its enforcement.
- **Three new `scripts/keel-verify` checks** (`references/phase-5-development.md` §1a): Keel conformance is complete (a `missing` row with no decision, or an applicable requirement with no row at all, fails the run); every user-visible acceptance criterion has either a driven test or a tagged delegation (the mechanical end of "the user will check it"); and interactive elements carry their test identifiers rather than binding to localized visible text.

### Changed

- **`references/playground-recipes.md` — rewritten and extended to every surface.** It was a WordPress / MCP / web / library catalogue; it now also covers Apple platforms (a dedicated simulator created for the project, the `build-for-testing` / `test-without-building` split, reading the result bundle instead of parsing stdout, and the accessibility audit that runs inside the UI test), headless Android emulators, Electron and Tauri, native Linux GUIs through a virtual display, native Windows through UI Automation, and interactive CLIs and TUIs through a real PTY. Every recipe now declares **whether its driver takes over the user's screen**, and the WordPress recipe records the settings that make it actually work — the debug log that is not enabled by default, the deprecated dual-environment start, the React checkout blocks that are not a form to fill, and the payment methods that are fully drivable versus the ones that need the user's own keys.
- **Phase 5 scaffold** runs `scripts/keel-doctor` before gate zero, and stands the drivers up with a trivial driven test per surface — because a driver that has never driven anything is not installed, it is merely present. Read-back (console errors, uncaught exceptions, failed requests, 5xx responses, the platform log) is wired from that first test.
- **Phase 5 test points** require the driven test to fill the real fields in three shapes and assert what the interface shows; a delegation without one of the eight tags is a slice defect.
- **Phase 5 sprint closes change shape.** The user no longer receives a numbered list of screens to visit and messages to confirm — that step is now explicitly called a defect in this phase. They receive what was verified and how, with its evidence; the short tagged list of what only they can do; and whatever needs their product judgment. Trying the build themselves stays available and welcome, as an invitation and never a duty.
- **Phase 7 gate** runs the doctor first (a green report produced by tests that never started is the worst possible release artifact), verifies the conformance sweep is current, and requires every user-visible acceptance criterion to have been verified by a driven test on the candidate — a criterion whose only evidence is "the user said it worked" is acceptable only where it carries one of the eight tags.
- **Adoption (`references/adoption.md`) gains step 4a, blocking:** the conformance sweep, enumerated from the manifest and never from recollection, with every `missing` row presented to the user in one uncurated batch and every refusal recorded as a decision. Applying stays the user's choice; proposing stops being optional.
- **The post-update reconciliation (`references/project-state.md`, `references/keel-maintenance.md`) becomes a sweep, not a summary** — every applicable Table 1 row and every Table 3 action newer than the baseline walked one by one, each landing in `docs/keel-conformance.md`, with the finished table reported to the user in full.
- **Phase 2 §4** — the technical plan's `## Testing` block now names a driver per surface with its headless verdict and mitigation, the element-addressability convention, the division of labour with its tags, the static analysis commands, the accessibility automation mechanism and the read-back duty; and the plan gains `## Environment requirements`, the source `keel-doctor` compiles from.
- **Permission allow-lists (`references/assistant-config.md`)** must cover the playground, the drivers, the sniffers, `keel-verify` and `keel-doctor --check`/`--plan` — and deliberately NOT `--fix`, because installing software on someone's machine should stop and ask every time. A driver command missing from the list is how the whole mechanism quietly reverts to asking the user.
- **The SKILL.md playground principle** now says plainly that trying the build is the user's option, never their duty.
- **Two more delegation tags, because six left real holes.** `PRODUCTION-RISK` (driving it would touch production data, money or a live customer system — the sandbox equivalent is driven first and only the live leg is delegated) closes the gap between this contract and the standing "when to stop and ask" rule. `NO-EXECUTION` (this session has no way to run commands where the repo lives) closes the biggest one: Keel runs on surfaces that have no shell, and without this tag the path of least resistance was exactly the behaviour being banned. Its rule is explicit — the tests are still written, with their exact commands, and handed to a shell-capable session through the continuation prompt; the work moves environments, it never turns into the user's clicking. `JUDGMENT` gained the scope rule it was missing: it is asked over the evidence AFTER the driven test passed, never instead of it.
- **Acceptance criteria carry stable `AC-nn` IDs** (Phase 2 §6) and `docs/05-test-points.md` carries two fixed-format columns, `Criterion` and `Coverage` (`driven` or one of the eight tags, never free text). Without them the coverage rule was self-declared prose; with them it is a grep over two ID sets and an enum, which is the difference this whole release is about. The old escape — "a one-line recorded justification" in place of a test — is gone.
- **Accessibility splits three ways instead of two** (`references/accessibility.md`): automated, **driven** (keyboard-only operation and focus order, focus restoration, focus visibility, tree snapshots — a sequence of key presses and assertions, which a machine does faster and exhaustively), and human (real assistive technology, and only that). The keyboard pass was in the manual column for no good reason; it moves, and the real assistive-technology pass batches per flow at sprint closes instead of being demanded slice by slice. The reference also states, once and correctly, why two different coverage figures circulate: about 57% of issue *volume* by Deque's measurement, far less of WCAG success criteria — and never quote the first as conformance.
- **Phase 4 stops being the phase where the user does everything by hand.** `SPEC/external-setup.md` is triaged into drivable (WordPress and WooCommerce settings, test products, offline payment methods, seed content — scripted into the seed, not read aloud to the user) and guided (tagged, one step at a time, only what genuinely needs a person). Fonts with a deterministic source are fetched and verified by script. The environment and the drivers stand up as soon as the first screen renders — a build nobody can run is a build nobody can verify — and every element built here carries its test identifier rather than being retrofitted in Phase 5. The fidelity walk verifies rendered output at the declared breakpoints instead of reading CSS.
- **Maintenance joins the contract** (`references/maintenance.md`), which matters because that is where most of a project's life happens: every session opens with `keel-doctor --check` and, if the baseline moved, the reconciliation sweep; reproductions are driven by the assistant rather than delegated to the reporter; the hotfix gate drives the fixed flow in the real environment instead of "install it and see"; and a released project that never had a playground or drivers gets them as its first maintenance change.
- **Adoption's gap audit gains a Testability row** — playground, drivers, doctor, test identifiers, criteria with driven coverage. On a released project the honest answer is usually "none of it exists", which is a high-value finding rather than an embarrassment.
- **Phase 6 product screenshots are captured by the assistant** from the playground, driven to the exact screen and state at a fixed viewport against seed data — deterministic, regenerable, and free of real customer data.
- **`references/anti-patterns.md` gains two traps and three self-audit questions:** "the test delegated to the user" and "Keel applied halfway, silently", plus the questions that catch them at sprint closes, at the Phase 7 gate and at adoption.

### Fixed

- **`playwright install-deps --dry-run` was described as a probe. It is not.** It prints the command it would run and says nothing about whether anything is missing; a doctor reading its exit code as "all good" certifies a broken environment. The reference now uses it only to populate `--plan`, and detects browser dependencies by actually launching the browser.
- **The WordPress recipe recommended a setting that breaks its own PHPUnit strategy.** `testsEnvironment: false` silences the dual-environment deprecation warning but also stops the tests containers from existing, so `wp-env run tests-cli ... phpunit` has nothing to run against. The recipe now makes the choice explicit — keep the tests environment, or use a separate config file — instead of copying an e2e-only example into a project that also runs integration tests.
- **The Apple preflight listed detection commands as needing sudo.** `xcode-select -p`, `xcodebuild -license check` and `-checkFirstLaunchStatus` need no privileges, and `--check` must never ask for a password; detection and remediation are now separated.
- **`docs/keel-conformance.md` is created at Phase 1 step 0a**, so a greenfield project has the file the Phase 7 gate and `keel-verify` expect, starting almost entirely `missing` and emptying as phases land. The manifest, the phase and the definition of done now agree.
- **The v5.0.0 row of MANIFEST Table 3 was outside the table** (a stray blank line), which would have hidden the entire reconciliation delta from the mechanism built to stop exactly that.
- **Phase 5's definition of done still required "the user's recorded test verdict"** — the behaviour this release names a defect. It now requires the assistant's driven verification report with its evidence, plus the user's answer on the tagged list only.
- **The Phase 7 doctor gate treated any missing requirement as invalidating**, ignoring severity; it now applies to blocking requirements only.
- **Composer's root behaviour was described as a refusal.** It warns, disables plugins and — interactively — prompts, which hangs an unattended probe; the fix is the environment variable, and the reason is the hang, not a failure.

## 5.1.0

A headless test run is invisible, and an invisible run that reports "passed" asks the user to take the assistant's word for it — which is the same trust problem v5.0.0 set out to remove, wearing a different hat. This release closes it: the run stays headless by default, but it now always leaves something a human can open, and watching it live becomes a flag instead of a wish.

### Added

- **Video and trace are the evidence on every browser surface** (`references/test-automation.md`, `references/playground-recipes.md`). Playwright configs set `video` and `trace` on, artifacts land in the ignored artifacts directory, and the test-point row carries the trace path. A trace is a timeline where every action holds a DOM snapshot before and after it, the selector used, the network call it triggered and the console at that instant — rewindable, and readable months later by someone who was not there, which live-watching never is. A driven check on a browser surface whose row has no trace path is now incomplete in exactly the way an unrecorded command is.
- **The headed mode, documented as a script.** `headless: false` plus a `slowMo` a person can follow, reachable as `test:e2e:watch` rather than an edit to the config, with `--ui` and `--debug` named for the interactive runner and stepping. Wanting to see the browser fill the fields is a legitimate thing to want and it costs one flag.
- **Three declared browser run modes** (`references/playground-recipes.md`): headless as the default, headed on demand, recording always. The technical plan's `## Testing` block gains a "Run mode and recording" line, and the Phase 2 gate fails a browser surface with no trace or video configured.
- **The sprint-close report hands over the trace, not just its filename** (`references/phase-5-development.md`) — the path plus `npx playwright show-trace <path>`, because a trace nobody knows how to open is a passing test with extra steps. The headed run is offered in the same breath, with its two honest caveats: it proves nothing the recording did not, and it takes the screen while it runs.

### Changed

- **The drivers-by-surface table now tells the truth about headed runs.** Web UI and WordPress rows read "No by default; yes while a headed run lasts" instead of a flat "No" — the screen-stealing verdict is a property of the mode, not only of the platform.
- **Offering a headed run is a courtesy and never a substitute** (`references/test-automation.md`). Skipping the recording because the user could watch, or answering a doubt about coverage by inviting the user to run the suite themselves, is the delegation v5.0.0 banned re-entering through the door marked transparency.
- **The WordPress recipe notes that WooCommerce ships its own Playwright utilities** alongside the WordPress ones, so a project checks which fixtures it actually needs rather than adding both by reflex.

### Fixed

- **The delegation tag list in `references/phase-2-functional-spec.md` still had six tags, not eight.** `PRODUCTION-RISK` and `NO-EXECUTION` were added to the contract in v5.0.0 but never reached the technical plan's `## Testing` template or the Phase 2 gate, so the two tags that close the biggest holes were invisible at exactly the moment a project writes its division of labour down.

## 5.2.0

Keel already sent the right checks to the right verifiers. It just ran them in a queue. Every gate read as a sequence of independent passes, each waiting on an answer it never needed from the one before it, and a twelve-screen fidelity walk was twelve serial checks inside a single agent. Nothing was wrong with the verification; the wall-clock was simply the sum of things that had no reason to be added together. This release makes parallel the default and serial the exception that has to justify itself.

### Added

- **A canonical fan-out rule** (`references/assistant-config.md`, "Parallel fan-out"), defined once beside the nine verifiers and pointed at from every phase that invokes them — the same define-once discipline the agents, the rules and the model binding already follow. It names the two shapes fan-out takes and the only four cases where serial is still correct.
- **The reading/executing split that makes parallelism safe.** `code-reviewer`, `security-auditor`, `docs-verifier`, `guide-qa` and `design-fidelity-auditor` only read, so any number of them run together. `playground-qa`, `test-driver`, `a11y-auditor` and `launch-verifier` also EXECUTE, and executing means holding a shared environment — containers, ports, one database, one seed dataset, one origin — so at most one of them runs per environment, with the reading block alongside. Two agents holding one playground is not parallelism, it is contention: a teardown landing mid-suite makes `test-driver` report connection failures and 5xx the product never had, and the rules then oblige it to fail on them.
- **Per-unit fan-out where the work is one check repeated over independent units:** one `design-fidelity-auditor` per screen at the Phase 4 fidelity walk, one `guide-qa` per locale at the Phase 6 guide check, one agent per audit dimension in the adoption gap audit, one reviewer per rubric domain at Phase 2 §6a where a rubric is already on record. Phase 1's one-subagent-per-competitor scan was already exactly this, and is now named as the precedent rather than left as a local habit. The rule carries its own guard: **a per-unit split never silently drops the cross-unit checks** — one extra whole-artifact agent takes them, or they disappear with the split and nobody notices. The worked example is the guide, where secondary-locale mirroring and the theme checksums are exactly what no single-locale agent can see.
- **Capture once, compare in parallel** — the rule that keeps per-unit fan-out from colliding with the one-executing-agent-per-environment limit. Where the repeated check needs a running environment, ONE pass produces the artifacts (the rendered screens at the declared breakpoints, the crawl output, the tooling report) and the per-unit agents read those artifacts concurrently, as readers. That is what makes a twelve-screen fidelity walk one round instead of twelve without putting twelve agents on one browser, and it is why `design-fidelity-auditor` stays a reader: Phase 4 already captured the screens before it is dispatched.
- **Merging as an explicit duty of the main session.** Each agent returns findings for its own slice and nothing else; the session deduplicates by `file:line` and reports once, and the gate is judged against the merged set — never against whichever agent answered first, and never against a partial set because one is still running. This is the failure mode a fan-out introduces that a chain never had, so it is written down where the fan-out is.

### Changed

- **The Phase 7 gate names its verifier set and dispatches the reading half in one block** — `security-auditor` and `guide-qa` over one frozen candidate — while anything that executes against the candidate runs on the built distributable, one at a time. Two bullets that read like a sequence become one dispatch with a stated reason: the candidate does not change between them.
- **Phase 5 sprint closes run `docs-verifier` and the playground-QA pass together** (one reads, one holds the playground), and a slice test point sends every reading verifier the gate calls for at once.
- **The Phase 8 launch checklist runs `launch-verifier` and `a11y-auditor` concurrently** over the same deployed site, with one guard: they are two full crawls of a single freshly deployed origin, so any `429` or `5xx` is re-verified single-threaded before it becomes a `launch-report.md` row. A throttled request and a broken page look identical in a table.
- **The hotfix gate fans out** (`references/maintenance.md`). Its step is titled "the full gate, fast" and was the one place where speed is the stated goal and the speed rule was absent. Fast now means fanned out, never trimmed.
- **The adoption gap audit fans out by dimension** (`references/adoption.md`) — independent reads of a tree nobody is modifying, and the largest one-shot verification a project ever gets. Two rows stay outside the block: Testability starts things, so it runs alone, and the threat model is authored by the session from the merged findings, because producing `docs/threat-model.md` is writing and no verifier here writes.
- **The portability lock gains the delegation half of its batching rule** (`references/project-state.md`). It already said to batch independent tool calls; now it says the same about independent verifiers and independent units, so the discipline reaches every assistant through `CLAUDE.md` / `AGENTS.md` instead of only the sessions that read the skill. Existing projects pick it up at the next lock-freshness refresh — which is why this ships as a version rather than as an edit.
- **SKILL.md now says the fan-out rule applies whether or not the assistant config package was accepted.** Both pointers described `references/assistant-config.md` as optional, which would have made the dispatch rule invisible to any project that declined the package — but subagent availability is a property of the environment, not of that package.
- **The no-subagents fallback is stated in these terms too:** where the environment has none (Codex, Windsurf, or any tool without them), the checks run inline and therefore serially. That is a property of the environment, not a licence to drop any of them.

### Fixed

- **The subagent section had counted eight verifiers since v3.1.0, and there are nine.** `test-driver` was added in v5.0.0 without updating the heading — in a release whose entire subject is the verifier set, the wrong count is not a cosmetic error.
- **`test-driver`'s recorded job needed a write tool its `tools:` line never granted.** Its definition says it "mechanically adapts" selectors, waits and fixtures, while the section above it said every executing verifier "still never writes" and the skeleton is `Read, Grep, Glob`. It now carries `Edit` scoped to the test tree, and the never-writes rule states the exception instead of contradicting the agent three paragraphs below it. This mattered here because the fan-out's ordering exception is built on that adaptation being real.
- **The Phase 1 competitor scan was missing from the list of places that invoke the agents**, even though it is the oldest fan-out in the skill and is now cited as the precedent for the per-unit shape.
- **`references/accessibility.md`'s `a11y-auditor` invocation** — the canonical one, loaded from Phase 1 as cross-cutting — had no dispatch guidance while the same agent's Phase 8 invocation did.
- **The conditional-agent list omitted `test-driver`**, which is conditional by its own definition ("any project with a runnable surface") — the same v5.0.0 oversight that produced the eight-verifier heading.
- **Phase 4 never named `a11y-auditor`**, though the agent's own description says "use before Phase 4's definition of done". Its automated pass is now the single executing agent alongside the per-screen fidelity block.

## 5.3.0

Chats stop handing over by copy-paste. A session that ends mid-work already composed a self-sufficient continuation prompt and showed it; now it also writes that prompt to a fixed path in the repository, so the next chat can be started by pointing at a file instead of by moving a wall of text between two windows. Everything else in this version exists to keep that file from becoming a lie — including the parts that were written wrong first and corrected by testing them on a real machine before release.

### Added

- **`docs/continuation-prompt.md` — the continuation file.** Written at every session end and every sprint close, always at that exact path, overwritten each time, and listed in `.gitignore` because it is ephemeral session state rather than project history. The fixed path makes the hand-off addressable by a short constant instruction and removes every length limit from what can travel between chats, because what travels is a path. Showing the prompt in the chat does not stop — the file is in addition, never instead.
- **A freshness header**, carrying `Repo`, `Generated`, `Keel`, `Commit`, `Tree`, `Position` and `Handover` (`clean`, or `blocked:` with the reason). The timestamp lives in the header rather than in the filename deliberately: a dated filename would change the path on every write, and the header plus the file's own modification time answer "was this written just now or three days ago?" at no cost.
- **`Repo` — the root-commit hash — and an ABSOLUTE launch path, which are one fix in two halves.** The filename is identical in every Keel project. A chat opened in the wrong window therefore reads THAT project's hand-off, where the commit really is its `HEAD`, the position really is its `PROGRESS.md` and the timestamp really is recent: nothing is stale, and the session continues the wrong project coherently and unsupervised. So every launch passes this repository's absolute hand-off path, making the right file get read; and `Repo` — the stable identity of a repository, unlike a remote URL, which may be absent or plural — makes a session that finds itself elsewhere stop and say so. Neither half is sufficient alone, which is why both are mandatory.
- **The courier rule, with its checks graded honestly.** The file is a courier and never a source of truth. Five checks are mechanical and a script can enforce them — containment first (see below) — `Repo` against `git rev-list --max-parents=0 HEAD`, `Commit` against `git rev-parse HEAD` (with `git merge-base --is-ancestor` reporting how far behind, which beats a yes/no), and `Generated` against both the clock and the file's `stat` mtime, which the header cannot forge. `Position` is a human-readable corroborator and is explicitly NOT claimed as a check: prose against prose is not executable, and a check that cannot be run is a promise. A missing file is not an error at all.
- **Optional chaining of the next chat, and the `Chaining:` project-card line** — `off` (the default), `prefill` (the next chat opens with the prompt already typed; the user presses Enter) or `start` (it launches and submits). The values name the BEHAVIOUR rather than the mechanism on purpose, because one tool family does both: Claude Code's URI handler pre-fills and never submits, while its CLI, started with a prompt argument, submits immediately. A single value meaning different things on different tools is the ambiguity this skill refuses everywhere else.
- **A tool registry with three evidence tiers** — VERIFIED, DOCUMENTED-BUT-UNTESTED, NONE FOUND — because "documented" and "verified" are not the same claim. Only a VERIFIED row is ever acted on; documented-but-untested is a lead telling whoever verifies it where to start. Claude Code holds both verified rows today (VS Code extension → pre-fills; standalone CLI → submits, and inherits `cwd`, so it cannot land in the wrong repository). Cursor, Codex and Gemini CLI are recorded as documented-but-untested with what their documentation actually says; Copilot and Windsurf as none found. An unrecognised tool is never a failure — it is the print row.
- **A containment check, because a repository is not a directory.** Two checkouts of the SAME repository at the same commit — a `git worktree`, a second clone, a copied or synced folder — share their root commit AND their `HEAD`, so `Repo:` and `Commit:` both pass while the session works in the wrong directory. That is the original flaw in a narrower disguise, and worktrees are not exotic: agent harnesses create them routinely. The hand-off's real path must lie inside this session's own `git rev-parse --show-toplevel`, and that check runs FIRST because it is the only one that separates this case. Demonstrated on a worktree, a second clone and a plain copy, all of which passed every other check.
- **`Tree: clean` / `dirty (N files)`**, from `git status --porcelain`. `Commit:` says nothing about uncommitted work, and the session most likely to leave some is precisely the one writing a hand-off while running out of context — so the next session would inherit changes it believes do not exist.
- **`scripts/keel-handoff-verify` — the courier checks stop being prose and become an artifact.** A session composing them itself writes one line of nested `$(...)`; the permission matcher cannot decompose it, returns `Parse error`, and the project's allow-list is bypassed even though every git command in it is allowed — at every link of a chain, not only the first. Measured twice in real sessions. The five checks therefore live in a generated script with ONE allow-list entry, `Bash(./scripts/keel-handoff-verify:*)`, printing a line per check plus `VERDICT: CONTINUE|STOP`. Keel already does exactly this with `keel-verify` and `keel-doctor`, and its own rule says a check that cannot be run is a promise — which is what the courier checks were until now.
- **The single-lane lock, and `start` gated on it.** Chaining was tested rather than reasoned about, and a three-link chain produced FOUR live sessions and four windows in sixteen seconds: two launches were still in flight while the counter was read, so the cap was passed by design. Two sessions on one checkout means interleaved commits on one branch, `PROGRESS.md` overwritten by whoever finishes last, and edits made on reads the other already invalidated — and no courier check fires, because it is the same repository, the same starting commit and a genuinely fresh hand-off. The lock is taken by the ARRIVING session (a launcher cannot know who else is in flight — precisely what failed) and lives OUTSIDE the repository (a brake the chain can `git clean` is not a brake). Until a project has it, `start` is not offered — as a gate, not a warning.
- **`/clear` in the same window, named as the common case it is.** The skill spoke only of "the next chat" and never of the situation that actually occurs most: the window is fine, the context is full. `/clear` starts a fresh session in place and then reads the hand-off — same window, same repository, permissions already granted, the wrong-window failure impossible by construction. It is the safest route and the least automatable (nothing lets a session clear its own context), which is why it is now stated rather than left implicit.
- **The `Chaining:` question is asked at Phase 1 with its warning attached**, in full wording, instead of being filled in as a silent card field. Two things the user must know BEFORE choosing: `start` moves development to CLI sessions rather than their editor (the VS Code URI pre-fills and its handler accepts no parameter that changes that), and `start` removes the only participant who can notice the chain has forked.
- **`scripts/keel-continue` and its contract**, generated at the Phase 5 scaffold on projects whose card is not `Chaining: off`: resolve the repo root and emit an absolute path, refuse a missing or `blocked` hand-off, detect the tool, downgrade to what the card allows but never upgrade, fall back to printing on any non-zero exit, and exit 0 on every path that leaves the user with a chat or a prompt. It writes nothing and decides nothing, which is what keeps the mechanism testable without any editor at all.

### Changed

- **A blocked close-out never chains.** If a session ends on any "When to stop and ask" row, a failed test point, an open Design Request, or the three-attempt rule, the file is still written — with `Handover: blocked` and the reason — and nothing is opened. Chaining a blocked state hands the next session a problem dressed as an instruction, and the next session cannot tell the difference.
- **`start` is opt-in per project, and the reason is stated rather than assumed.** The protections that matter survive without a human keystroke: a blocked hand-off never chains, and a stale or foreign one is refused on reading — all machine checks. What a keystroke uniquely guarded against was landing in the wrong repository, and the absolute path plus `Repo` close that instead. What remains is real but smaller: a window begins working unsupervised from a hand-off composed by a session that was running out of context. That risk is not uniform across projects, so it is the user's call, recorded on the card and in `docs/decisions.md`.
- **`.gitignore`'s unconditional entries** now include `docs/continuation-prompt.md`, alongside `CLAUDE.local.md`, `.claude/settings.local.json` and `.keel-update-check` — in all four places that enumerate them: the Phase 5 scaffold, `references/assistant-config.md` (entries list and definition of done), the manifest's Table 1 row, and the Phase 7 `.gitignore` section.
- **The Phase 5 sprint close-out (step 9)** writes the file and sets `Handover:` as part of the close, and chains only when the card allows it and the hand-off is clean.
- **The portability lock carries the duty, as its own UNBREAKABLE numbered item.** `CLAUDE.md` and `AGENTS.md` previously mentioned the continuation prompt as a trailing clause of the commit item; it is now item 5 in its own right — produce it, show it, write it, verify one before acting on it, chain only when allowed and clean, and never end without leaving something to continue from. It says explicitly that running low on context is when this is most likely to be skipped and most expensive to skip, so it happens BEFORE the session is exhausted rather than as an afterthought. This is what makes the duty reach environments where the Keel skill is not installed at all. The lock block changed, so every project refreshes it at the next lock-freshness check.
- **`Chaining:` joins the project-card template** in `references/project-state.md`, not only the manifest's list of card lines.
- **The hand-off is tool-agnostic; only the auto-open is not, and the skill now says which is which.** `docs/continuation-prompt.md` needs no integration from anybody: "read this file and continue" is an ordinary prompt, so Codex, Copilot, Cursor, Gemini CLI, Windsurf, a web chat or a human typing it all consume it identically.

### Fixed

- **The VS Code surface was identified by an environment variable that is not set there.** The registry's first draft matched `TERM_PROGRAM=vscode`; measured on macOS, Claude Code's VS Code extension sets `CLAUDECODE=1`, `CLAUDE_CODE_ENTRYPOINT=claude-vscode` and the `VSCODE_*` family, and `TERM_PROGRAM` is unset — while the standalone terminal sets `CLAUDE_CODE_ENTRYPOINT=sdk-cli`, `TERM_PROGRAM=Apple_Terminal` and no `VSCODE_*` at all. The condition could never have matched, so detection would have silently fallen through to printing. It failed safe, but it was a guess, made in the same version that forbids guessing about other vendors' tools — which is why the rule "a row is promoted only from an observation on a real machine" is now written beside the table with this mistake named as its reason.
- **The two branches were the wrong way round.** The URI branch was promoted as the safe default and the CLI branch gated behind verification; testing showed the URI neither automates (it waits for Enter, correctly) nor targets the right repository (it lands in the active editor window), while the CLI submits and inherits `cwd`. The gate was still the right call: verifying it is what revealed that the CLI submits outright, so offering it as an equivalent of pre-filling would have been a false promise.
- **"Only Claude Code has an action" was true of Keel's registry and false about the world.** Cursor documents the same mechanism with the same semantics. The claim is now scoped to the registry, and Cursor is recorded at its real evidence tier instead of being lumped in with tools that have none.
- **`Chaining: vscode` survived in the Phase 5 sprint close-out** after the values were renamed — a condition that could never be true, so a sprint close would never have chained at all.
- **The `.gitignore` entry was missing from the Phase 7 release section**, the fourth place that enumerates the same exclusion list.
- **Two commands were conflated where they do different jobs.** Firing a URI (`open` / `xdg-open` / `Start-Process`) serves `prefill`. Opening a VISIBLE CLI session is something else: on macOS `osascript` driving Terminal.app, verified; on Linux and Windows, unverified. An implementer reaching for the plain open command to satisfy `start` ships the headless variant, which runs the next session correctly and opens no window — the user waits for a chat that does not exist and nothing reports an error. `start` therefore resolves on macOS only today, and says so.
- **The `Generated:` check was called mechanical while being a string comparison.** The header writes `+02:00`, `stat` reports `+0200`; the same instant compares unequal, so a script reports tampering on every single run — a model reading them silently does the right thing and never surfaces the trap. Both sides normalise to epoch first.
- **`Repo:` had three silent failure modes in the command that produces it.** A SHALLOW clone returns the grafted commit rather than the root, with exit 0 and no warning, and the value changes after `git fetch --unshallow` — so a hand-off written in CI under `--depth 1` carries an identity no full clone accepts, and the same directory rejects its own hand-off once deepened; `git rev-parse --is-shallow-repository` now gates it and the field is written `unavailable (shallow)`. A repository with TWO root commits prints several lines, which a naive `$(...)` turns into an 81-character value that can never match a 7-character header; `| sort | head -1` picks one stably. And `Repo:` was described as identifying the working directory when it identifies the repository — corrected, with the containment check added to cover what it cannot.
- **The CLI registry row was pinned to the one entrypoint value that had been measured.** `CLAUDE_CODE_ENTRYPOINT=sdk-cli` came from a session launched programmatically; a person typing `claude '<prompt>'` reports `cli`, so the row would have missed the most ordinary case there is, and the vendor may add more. Matching is now asymmetric: `claude-vscode` by name, because it is the only value whose behaviour differs, and everything else with `CLAUDECODE=1` treated as the CLI. Measured on macOS in the process: the extension sets NO `TERM_PROGRAM` while the integrated terminal sets `TERM_PROGRAM=vscode` — so that variable identifies the terminal panel, not the extension, the exact inverse of the first draft's guess — and `VSCODE_*` turns out to be two disjoint families (`VSCODE_PID`/`VSCODE_IPC_HOOK`/`VSCODE_ESM_ENTRYPOINT` in the extension host, `VSCODE_GIT_ASKPASS_*`/`VSCODE_INJECTION` in the terminal). Three surfaces, overlapping variables, one reliable discriminator.
- **The CLI registry row excluded on `VSCODE_*` and would have stopped matching inside VS Code's integrated terminal**, a shell that inherits the editor's variables. The behaviour is a property of `CLAUDE_CODE_ENTRYPOINT`, not of the parent process: the CLI submits and inherits `cwd` whether it was launched from a terminal app or from a panel in the editor. Matching is now on the entrypoint alone, with `VSCODE_*` demoted to a corroborator that is never used to exclude. The integrated shell's entrypoint value remains UNRECORDED rather than assumed — it either matches the CLI row or prints, and both are safe.
- **The `keel-continue` contract contradicted an UNBREAKABLE rule and left three things to invent.** Its point 2 said to refuse a `blocked` hand-off and print why, while the prompt rule says the prompt is printed whenever a chat cannot be opened, for ANY reason — an implementer had to guess. It now prints the reason AND the prompt, marked blocked, and exits 0: refusing to chain and refusing to print are different things. "Downgrade, never upgrade" was unexpressable when the tool's row has no action at the resulting tier (the CLI row knows only `start`), which now prints. And the contract verifies containment, `Repo` and `Commit` BEFORE firing rather than after — under `start` there is no human between the launch and the work, so discovering a mismatch post-launch is the wrong order. Percent-encoding, the per-platform clipboard commands, and "a malformed header is treated as a missing file" are now specified rather than left to the implementer.
- **`scripts/keel-continue` was required by the manifest with nothing defining its contents**, unlike `keel-verify` and `keel-doctor`. Declaring a required artifact without a template is the "declared is not delivered" defect this skill exists to prevent, committed inside the version that hardens that rule; it now carries a six-point contract.

## 5.3.1

Two artifacts v5.3.0 required and never told anyone to build. Nothing new is designed here; what was specified is given a place to be created and a moment to be created in.

### Fixed

- **`scripts/keel-handoff-verify` was required by the manifest and absent from the reference that builds it.** v5.3.0 named it in Table 1 and described its contract in `references/project-state.md` — "generated at the Phase 5 scaffold beside `keel-verify` and `keel-doctor`" — but never mentioned it in `references/phase-5-development.md`, which IS the Phase 5 scaffold. By this repository's own authority rule (the phase references are the contract; where they disagree with the manifest, the reference wins) the artifact was therefore never created on a new project. Only the post-update reconciliation created it, which produced the perverse result that projects predating v5.3.0 had the script and projects started after it did not. The scaffold now generates it, and generates it UNCONDITIONALLY: every project writes `docs/continuation-prompt.md` at every session end whatever `Chaining:` says, so every project needs the script that verifies one before acting on it. Its single allow-list entry goes into every capable tool's container at the same moment — that entry is not an optimisation but the thing that makes the checks executable at all, since a session composing them inline produces nested `$(...)` that the permission matcher cannot decompose.
- **The single-lane lock was a gate with no key.** v5.3.0 specified the lock, made it the precondition for offering `start`, and named nobody to build it and no moment to build it in: its Table 1 row said "Before `start` is offered", which is a condition and not a step, and no reference said who creates it. `start` was therefore gated on something no procedure produced. The lane is now claimed by `scripts/keel-handoff-verify` itself, generated at the same scaffold — the arriving session already runs that script before acting, which is exactly when the lane must be claimed, and doing it there leaves no window between verifying and starting work in which a second session could slip through. One file outside the repository, keyed by the real path of `git rev-parse --show-toplevel`, holding PID and start time. No separate script, no step for anyone to remember, and one allow-list entry instead of two.

Both failures are the same shape and it is worth naming: a requirement recorded in the index and missing from the contract. The manifest's authority rule exists precisely because that shape recurs — it decides which side wins, and in both cases the side that wins was the silent one.

**The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.3.2

One unstated dependency. `start` launches a CLI session, so it always needed the `claude` command on PATH, and v5.3.0 shipped that requirement without writing it down anywhere.

### Fixed

- **`start` names the CLI as its fourth requirement.** The gate listed three (the single-lane lock, a verified visible-session action, the allow-list entry plus folder trust) and omitted the one without which there is nothing to launch at all. It is worth stating explicitly rather than assuming, because it is genuinely not obvious: neither the desktop app nor the VS Code extension puts `claude` on PATH — the app runs Claude Code graphically, and the extension bundles a private copy for its own panel — so someone can have both installed, use Claude Code every day, and still have no `claude` command. Left unstated, the failure surfaced at the first close-out of a chain, which under `start` is precisely when nobody is watching. A project missing it is offered `prefill`, told which requirement failed, and nothing else changes.

**The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.3.3

Two behavioural fixes, both found by building the thing that would have exercised them. Nothing new is designed and nothing migrates.

### Fixed

- **The conformance sweep was reading its own previous output.** Run as a real fan-out over a project, the sweep's agents cited the `docs/keel-conformance.md` already on disk as evidence for their own verdicts. That makes it self-confirming: a row that was wrong reproduces itself with the air of evidence, and the one artifact that decides whether Keel is applied gets more confident with every run instead of more correct. The sweep's authorised sources are now exactly three — the disk, `MANIFEST.md`, and `docs/decisions.md` — and it overwrites that file rather than consulting it. This is not a property of fan-out; a workflow merely made it visible, and it applies identically to a subagent block or to one session walking the list inline, which is how it runs on every project today.
- **The single-lane lock had no release and no recovery policy.** v5.3.1 specified that the lock holds a PID and a start time "so a stale lock from a crashed session is detectable rather than permanent" — which states a property and stops short of a policy. Detectable by whom, and then what? The holder now releases the lane on EVERY exit path, and because a killed process runs no cleanup and an orphan is therefore inevitable, the arriving session TAKES a lane whose PID is dead (or whose start time no longer matches, since PIDs get reused), says in one line that it recovered an orphan and from which PID, and continues. It does not ask: a stale lock that needs permission to clear is a lane nobody can enter, which is the same outage as a lane nobody leaves. A live PID still means busy, and nothing is taken — that case is the lock working.

**The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.4.0

One idea: Keel learns what its environment can actually do, and orchestrates accordingly. The preflight stops assuming, the dispatch rule gains the step above subagents, and a round of orchestration research is banked as three rescued findings and three closed doors.

### Added

- **The environment preflight probes the `claude` command instead of assuming it.** Where the project card says `Chaining: prefill` or `start`, Phase 1 §5a now runs `command -v claude`, offers the platform's install command when it is missing (macOS, Windows and Linux commands are written down), and records the answer either way; declined or unavailable means `start` is not offered and `prefill` is the maximum, naming the requirement that failed. v5.3.2 named the CLI as the fourth gate on `start` and left nobody to check it, which put the discovery at the first close-out of a chain — under `start`, precisely when nobody is watching.
- **Protected environments are announced up front instead of discovered on failure.** The preflight now asks what this session's environment CANNOT do and says it in one line at Phase 1, beside the English-docs default and the accessibility commitment. The measured case is Cowork's device bridge, and every item was hit for real during v5.3.x: the bridge cannot delete files (`rm` returns `Operation not permitted`, and the `.lock` files git leaves then block the user's own repository), it has no network, it exposes no `localhost` of the user's machine, it offers no screen control, and the cloud container that DOES have network cannot see the user's files — so neither half can execute where the repository lives and reach the network at once, which is exactly what playgrounds, installs and test runs assume. A protected environment is a fact about the session, not a failure; saying it late is what makes it expensive.
- **`NO-EXECUTION` gains its partial case.** The tag described a session with no execution at all. The measured environment is neither that nor fully capable: it can execute, it can touch the files, but not both together, or not with a network. That case now has language — say which half is missing, tag per LEG and never per session, and drive everything the session genuinely can drive.
- **The verifier dispatch rule gains its three-step fallback chain:** workflow (where one exists and the user has opted in) → parallel subagent block (where subagents exist) → inline and serial. All three produce the same table; only the wall clock changes, and none of them drops a check. A workflow tool needs explicit opt-in, may be absent, and carries a user-controlled size cap Keel does not govern — identical in kind to the subagent situation the standing fallback already handles, which is why it is written as a chain and never as a requirement.
- **The conformance sweep is written up as the canonical workflow shape**, measured on a real project: 31 rows, 5 agents, 110 seconds, no hallucination found when the evidence was contrasted against the disk. With a per-row schema no MODEL sits between the data and `docs/keel-conformance.md` — but the claim is stated honestly rather than sold: two pieces are still needed, a deterministic template in the script (about six lines, no model) and a way to reach the disk, since the script has none — either a writing agent forbidden to change a word, or, cleaner and one agent cheaper, the workflow returns the markdown and the main session writes it. "No post-processing" means no model in the middle, not no code. Dedup becomes a function instead of a discipline, the concurrency cap is native, and the cross-unit check becomes an explicit extra agent visible in the script rather than trusted to memory. What stays judgment is said too: which verifiers a gate calls for (six of the nine agents are conditional), the reader/executor split, `test-driver`'s ordering exception, and the gate's verdict.
- **A rule for fanning work out over git worktrees, and it inverts a Keel rule on purpose.** Dispatched at the Phase 5 sprint kickoff, which names who dispatches, who writes the report and when (`references/phase-5-development.md` §0) — the rule itself lives in `references/project-state.md`, and the phase reference is what makes the artifact actually get created on a new project. The living state is written by the session that owns the MAIN tree; a worker in a worktree writes `docs/.keel/slices/<n>.json` (`slice`, `status`, `branch`, `commit`, `needs_user`) on its own branch and never the state. Measured: with the workers writing `PROGRESS.md`, the code merged clean every time and the state file conflicted in 100% of merges, N−1 times for N workers — with the report file instead, three merges and zero conflicts. `merge=union` in `.gitattributes` is recorded as dangerous rather than merely useless: a state file that is REWRITTEN rather than appended merges cleanly into a corrupt one, two contradictory position lines and no conflict marker. The worker's close-out contract is fixed in order — commit → report → atomic done-signal (`printf > tmp && mv`, because a rename cannot be read half-written) — so an existing signal implies committed work and never the reverse. Not stdout: `claude -p` writes nothing until the end, so a live worker and a dead one both show an empty log. A `blocked` report is not merged; its question goes to the chat where a person is. New conditional MANIFEST Table 1 row for the slices path.
- **`claude agents --json` is recorded as the supported way to see live sessions** — pid, cwd, kind, session id, name, and `idle`/busy — replacing any parsing of `*.jsonl` transcripts, with the caveat that must travel with it: **`-p` sessions do not appear**, only interactive and `--bg` ones, so it is not a completion detector for print workers. `--session-id <uuid>` lets a launcher assign the id up front.

- **The repository's own release linter gains check 9: every MANIFEST Table 1 row required AT A PHASE is named by that phase's reference.** This is the shape v5.3.1 fixed and that recurred inside this very release — a requirement recorded in the index with no phase reference telling anyone to create it, so by the manifest's own authority rule the artifact is never created on a new project and only the reconciliation produces it. Run against the v5.3.0 tree the check fails on `scripts/keel-handoff-verify`, turning a release that shipped "Releasable" into a blocked one; run against this tree it is clean. Two false negatives found while building it are worth recording, because any future check over these tables inherits both: splitting rows on `|` without unescaping `\|` shifts the columns and the check passes without verifying anything, and accepting a bare directory basename let `docs/.keel/slices/` through on the word "slices", which Phase 5 uses in every paragraph for vertical slices. Three rows are exempt through an explicit list rather than a generic two-hop rule — the convenient generic version lets the v5.3.0 defect through — and one gap stays open in the file rather than silently unhandled: rows whose "Required from" is an event rather than a phase have no owning reference to check against. Prose did not stop this shape twice; the check runs on every tag.

### Changed

- **File isolation is not environment isolation.** Where a harness offers to run each agent in its own worktree, what it isolates is FILES. The serial case it might appear to dissolve is about two EXECUTING verifiers sharing one port, one database, one seed, one origin — and two worktrees fight over all four exactly as two directories in one checkout would. "One executing verifier per environment" stays a serial stage whatever the isolation mechanism offers.

### Rejected — measured, and recorded so they are not re-proposed

- **The chat director — a second chat whose role is orchestrating worker chats — is abandoned as a design.** It works: three slices, three workers, zero conflicts, the state written by the director alone, and a product question escalated instead of invented. It still does not earn its scaffolding. It costs ≈7k tokens of context per slice in a toy and 15–20k in a real project — a `PROGRESS.md` of 4–18 KB read and rewritten every slice is what fills it, not the waiting loop, which is one Bash call returning one line however long it blocks — and therefore degrades between the 6th and the 9th slice. No cost accounting, no cancellation, no concurrency cap, all to obtain parallelism the native primitives already provide with all three. Its one real advantage, a chat with a person in it that can be asked a question and can answer, is obtained by something much smaller: one ordinary Keel session that, in a single turn, dispatches N workers into worktrees, waits for their signals and merges. The chat where the person is, is the chat you are already working in. The negative result is the useful result of the round, and the formula built to prove it is what was kept.
- **`claude --resume` as a mailbox into a live session.** There is none. It starts a new process that reads the transcript from disk: against an idle session the target's window sees nothing, and against a session mid-turn the transcript forks, a `Continue from where you left off.` / `No response requested.` pair that nobody wrote is fabricated, and the message vanishes silently from the resumable history. It is also scoped to the current directory's slug, so a worker in a worktree could not reach the main tree's session even if the mechanism worked.
- **Closing another session's window from a script.** Measured on macOS: 40–78 seconds with no confirmation and no error either way, a hard kill in which no cleanup hook runs at all (three signal traps installed, none fired, the log never created), and therefore a guaranteed orphaned lane, since the holder can never release it. Opening an unrequested window was already placed behind the user's decision; closing one is more destructive and the mechanics are worse. Not even opt-in.

**The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.4.1

One idea: v5.4.0 wrote down a fan-out and never wrote down how to start one, and it gated the CLI probe on the wrong question and then trusted the probe too much. Three fixes, no new artifact, nothing to migrate.

### Fixed

- **The fan-out gains its dispatch command, which v5.4.0 specified everything around and never wrote.** `references/project-state.md` recorded who writes the living state, the worker's report file, the close-out contract in its fixed order and the blocking wait, and left the reader with a contract to satisfy and no way to begin: neither `git worktree add` nor any launch appeared anywhere in the skill. The new "Dispatching a worker" section carries both, plus what the two flags are for — `--session-id` assigns each worker's id up front instead of looking it up afterwards, and `--permission-mode bypassPermissions` is what lets an unattended worker finish, which is exactly why a fan-out is dispatched only from a session with a person in it, only over slices approved at the kickoff, and only into worktrees of this repository. The log file is named as post-mortem reading and never as a completion detector, consistent with the `-p` measurement already recorded. Phase 5 §0 now points at the command and states the precondition it needs.
- **The CLI probe stops being gated on `Chaining:`, because the fan-out needs the same binary for a different reason.** Phase 1 §5a asked `command -v claude` ONLY where the card said `prefill` or `start` — so a project on the default `Chaining: off` could plan a fanned-out sprint and meet the requirement at dispatch, with the sprint already planned. This is the shape v5.3.2 fixed for `start` ("named the CLI as the fourth gate and left nobody to check it") recurring inside the release that introduced the fan-out. The step now asks both halves, is not applicable only when BOTH are false, and records the answer for both consumers: no working CLI means `prefill` is the maximum AND the sprint is built serially in the session — a slower plan, not a degraded one.
- **A probe's answer stops being taken as the machine's state, in both directions.** `references/test-automation.md` said to use `command -v` and never said what a negative is worth. It is worth little: an assistant's shell is frequently not the user's, and a restricted `PATH` — a bare `/usr/bin:/bin:/usr/sbin:/sbin` is the measured case — hides `php`, `gh`, `docker`, `node` and `npm` that work perfectly in the login shell, so the doctor offers to install what is already there. That is the same class of bug as collapsing `NOT OPERATIONAL` into `MISSING`, and it spends the user's machine rather than their patience. A negative now requires corroboration before it may be written `MISSING` — the login shell, the platform's install locations, and any variable the tool itself exports, with `CLAUDE_CODE_EXECPATH` (absolute path of the running binary) and `CLAUDE_CODE_VERSION` named for Claude Code, which resolve the CLI with no `PATH` at all — and a tool found outside `PATH` is present, with the row recording the path that works. The reverse is now stated too, and it was measured on a real install: `npm install -g` accepts a package whose `engines` demands a newer runtime than the active one and emits only an `EBADENGINE` warning, so the binary lands on `PATH` and whether it runs is still unknown. A positive is followed by the tool's own version probe against the requirement, and records `TOO OLD` or `NOT OPERATIONAL` rather than `OK`. Phase 1 §5a applies both directions and prefers the native installer, which bundles its own runtime, where the user's is managed by a version manager or a conda environment and can change under them.

**The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.5.0

Keel stops working in the dark: it settles what it is allowed to do before it starts, and it tells the developer when it stops.

### Added

- **`references/notifications.md` — out-of-band notification, so a stop reaches a human.** Keel halts on purpose ("When to stop and ask" makes stopping a successful outcome), but a blocked session and a working session look identical from outside: the chat sits there, the developer is elsewhere, and a thirty-second question costs an afternoon. The new reference defines the channel that closes that gap, and its central rule is **a channel counts only if it DELIVERS**. That is not a hypothetical distinction: the Gmail connector commonly available to a session exposes `create_draft`, `update_draft` and `list_drafts` and **no send operation**, so "notified by email" after creating a draft is a false claim of exactly the kind "declared is not delivered" forbids — and it fails precisely when nobody is watching, which is the only case the mechanism exists for. Candidates are therefore classified before use (delivering / compose-only / in-chat only), with **the environment's own notification tool as the preferred default** — no address, no credential, no server, so it is the only channel already true on a fresh machine — and two of its properties written down because both are easy to misread: it reaches the desktop always and the phone only while the harness's remote control is connected (so it covers "walked away from the desk", not "out of the building"), and it deliberately does not fire while the user is at the terminal, reporting "not sent", which is this reference's own anti-noise policy one layer down and therefore counts as delivered-or-redundant rather than as a failure to escalate. The capability is PROBED each session rather than read off a tool list (a connector can be listed and unauthorized; a headless or scheduled run usually has neither), and "no channel available" is a stated result rather than silence. The reference also fixes the triggers — a blocking stop, a blocking delegation tag, a deploy the developer must make, a blocked hand-off — and the anti-triggers, because the failure mode of a notification system is never too few messages but a person who has learned to ignore it. What the message carries is bounded by the confidential-data rule: a channel is a wider exposure than a repository, never a narrower one, so no credentials, no customer data, no source blocks — the project, the position, the one-sentence blocker, and where to continue.

### Changed

- **The session opens with three questions in one batch, asked once and recorded.** `SKILL.md` gains "Session start setup", executed with the maintenance block and before the entry-mode decision: automatic mode yes or no, whether Keel works the forge issues at every sprint close, and how it reaches the developer out of band. The answers land on new project-card lines — `Autonomy:`, `Branches:`, `Notify:` — plus a decision entry, and no later session asks again. **Everything hangs off the first answer, and it is never inferred per action:** automatic means Keel does not ask and performs every merge to the integration branch and every push itself; not automatic means the opposite regime in full — it asks every time and pushes nothing that was not explicitly requested, with the unpushed commits named in PROGRESS.md so the queue is at least visible.
- **`manual` permission mode is now resolved before any work, because it makes the rest of the skill impossible.** In Claude Code's default mode a command is matched STATICALLY against the `allow` rules, so anything composite — a shell variable, a `&&`, a `;`, a pipe — opens a dialog even when every command inside it is allowed, and Keel's phases are made of exactly those commands. The result is not slower work, it is stopped work. `references/keel-maintenance.md` gains the full procedure: the preferred persistent route (create or MERGE `.claude/settings.local.json` — `defaultMode: "auto"`, a minimal `deny`, `env.PATH` — never removing or reordering a rule the user put there), the per-session alternative (`--permission-mode auto`, or Shift+Tab), and the `.gitignore` verification. This is the single exception to "Keel never creates a personal config file", and it is narrow by construction: that file is machine-local and gitignored, so it binds this checkout and nobody else, whereas the COMMITTED `.claude/settings.json` still requires the user's explicit confirmation — `references/assistant-config.md` now states both sides so the two rules cannot be read as contradicting each other. The mode is the user's decision; writing the file that implements it is not a second question.
- **`env.PATH` is written expanded and absolute — never `${PATH}`, never `$HOME` (measured on a real machine, mid-release).** The field is consumed literally: a value ending in `:${PATH}` appends the seven characters `${PATH}` rather than the existing PATH, and `$HOME/...` names a directory that does not exist. What gets lost is the base system directories, so `/usr/bin` and `/bin` vanish and `git`, `ls`, `cut` and `grep` stop resolving in every session and every project until someone edits the file — and the symptom reads as `command not found: git` on a machine where git obviously works, which is precisely the false `MISSING` the doctor's corroboration rule exists to catch. The recipe now enumerates every directory literally, system ones included, and the procedure says to resolve the real value in the user's login shell and write the RESULT rather than the expression that produced it — plus to inspect any existing `env.PATH`, including the user-level `~/.claude/settings.json`, and repair an unexpanded one in the same breath, because that one poisons every project on the machine rather than a single repo. The earlier draft of this release shipped the broken form.
- **Keel never prefixes a command with `export PATH=... && ...` again.** With `env.PATH` in the settings file the tooling already resolves, and the prefix ALONE makes the command composite — forcing a dialog on every single call, including calls that would otherwise have matched an `allow` rule cleanly. The rule covers tool calls, generated `scripts/` files, phase references and commands handed to the user; an unresolvable tool is answered with its absolute path (recorded in the environment requirements table), never with a PATH prefix. The milder form — `cd <dir> && <command>` where the tool already takes a working directory — has the same fix.
- **A forge issue is answered in three beats, and Keel never closes one (UNBREAKABLE).** The previous rule tracked issues honestly and left the reply as a single event, which is wrong by two beats: the code changing and the reporter being able to try it are different events, usually days apart. So — (1) fix landed: comment what was wrong, that it is implemented, and that they will be told when it is testable; status `awaiting deploy`; **not closed**. (2) The DEVELOPER is notified through the recorded channel that a build or deploy is needed before the reporter can test — a real stop, recorded in PROGRESS.md, with Keel waiting. (3) The developer confirms it is up: comment again, naming the version and what to look for; status `awaiting reporter`. The reporter's answer closes it or reopens the work, and if the reporter or developer already closed it, Keel leaves it closed and records who did. `docs/issues.md` gains the two statuses and the `Replies:` / `Deploy:` / `Closed by:` fields, so a fresh session can tell beat 2 from beat 3 without reading the forge thread and guessing. Phase 5's sprint close gains step 8, and its inbound half is deliberately TWO sweeps — new issues, and **new comments on issues that already exist**, the half that is easiest to skip and most expensive to skip, because a log recording only what Keel published is a monologue: a comment on an issue in `awaiting reporter` IS the verdict that closes it or reopens the work, and it may not sit unread until someone notices. The outbound half cross-checks against commits and change-maps, never recollection — and a "fixed" claim needs the same evidence as any other claim in that phase: the commit and the regression test); `references/maintenance.md` carries the same lifecycle into post-release, where the gap between shipping and confirming is widest.
- **`develop` integrates, `main` is the person's, and the assistant does its own pushing (UNBREAKABLE).** `SKILL.md` gains a cross-cutting "Git flow" section that separates two things which get confused constantly: **publishing work so it is not lost is the assistant's job; releasing work to users is the person's.** Withholding a push does not create review — it creates a queue nobody owns, invisible to every other checkout and dependent on someone remembering; the observed symptom was a PROGRESS.md carrying several entries marked "PENDING push". So: the integration branch (`develop`) is created from `main` and published if absent; work branches start from it, never from `main`; in automatic mode the assistant merges them back and pushes, both halves, without asking; and the assistant **never** merges `develop` into `main`, never tags and never publishes a release on its own initiative — those need an explicit instruction in the current conversation, the same shape as the version-change policy and for the same reason: they are what real users receive. When `develop` holds work ready for production, Keel says so under "Ready for `main`" in PROGRESS.md and in the hand-off, and stops there — signalling IS the action. One deliberate exception, which is not a loosening: a production hotfix branches from the RELEASE TAG (users run the tag), merges back to `develop`, and still never reaches `main` without the user. Phase 5's sprint close gains the merge-and-push step, Phase 7 opens by naming who performs the release, and `references/maintenance.md` reconciles the hotfix branch point. **The rule is checked, not merely written:** `scripts/keel-verify` now fails on commits that never left the machine (`git log @{u}..HEAD`) and on working directly on the release branch — a rule stated only in prose is the trap this skill names elsewhere, and it would have been self-inflicted here. `scripts/keel-handoff-verify` gains the matching `Branch:` row, because the hand-off header grew a field and a field with no check is a promise. **Adoption is not exempt and not steamrolled:** `references/adoption.md` runs the same setup batch, and settles the branch shape WITH the user against what the repo already does — conventions are observed, not imposed — with only "the assistant never merges to the release branch, never tags, never releases" held non-negotiable.
- **Keel finishes the queue instead of asking whether to continue.** A new operating principle: when a block of work ends and the queue holds work that does not depend on what was just done, continue — do not stop to ask, and do not hand back a menu of what is left. Exhausted context is a hand-off, not a decision: write `docs/continuation-prompt.md` and carry on there under the same rule. Keel stops for exactly two reasons, both already in "When to stop and ask" — the next step depends on something not yet done, or the decision is genuinely the person's. "Shall I go on?" is neither; it is the assistant asking the user to do its scheduling, at the cost of a round trip every time.
- **The hand-off says where the code is.** The continuation prompt gains a `Branch:` line in its freshness header (work branch, whether it is merged and pushed, anything awaiting the user's merge to `main`) and three new opening steps: apply the recorded setup rather than re-asking it, read the branch state, and keep going while the queue holds independent work. A hand-off that does not say where the code lives sends the next session hunting for it.
- **One close-out opens one window: the launch receipt (UNBREAKABLE).** Measured on a real close-out — a single session fired the launch four times, four windows opened, three arrived, found the single-lane lock held and politely exited. The mechanism worked exactly as specified and the user still got four chats with no way to tell which was live. The cause is a gap the old design stated openly: the lane serialises **execution** and nothing serialised **launching** (`keel-continue` explicitly did not take the lock, on the grounds that arrivals would sort it out). So the launcher gets its own guard, and it protects an EVENT rather than a resource: a launch is claimed against the hand-off's identity (`Generated:` + `Commit:`), the claim is an atomic `mkdir` rather than a `test -f` followed by a `touch` — the gap between those two is where the duplicate is born — and the receipt lives outside the repository keyed by the real toplevel path, like the lane, so a `git clean` cannot erase the brake on the thing it brakes. Three rules complete it: **a launch is never retried** (an open command's exit code says the command ran, not that a window appeared, and firing again under that uncertainty is exactly how one hand-off becomes four — the answer to "I do not know" is always to print the prompt); **exactly one place in the skill may invoke `keel-continue`**, so a close-out that re-runs hits its receipt and fires nothing; and **a circuit breaker** stops chaining and says so when more than three receipts appear for one repository within an hour, because a loop that announces itself costs a message and one that does not costs a screenful of windows. The two guards now state plainly that they are not substitutes: the lock decides who may WORK, the receipt decides how many windows OPEN.
- **`scripts/keel-doctor` gains a third severity, `advisory`, and its first two rows.** Permission mode and notification channel are reported, never blocking: the work completes either way, but under friction the user should know about. Collapsing them into `blocking` would fail gates over a preference; leaving them out would let a session in `manual` mode degrade the driven-test protocol back into asking the user, which is the failure that reference exists to prevent.
- **The continuation prompt applies the recorded setup instead of re-asking it.** Its first step is now to read the card's `Autonomy:` and `Notify:` lines, resolve the permission mode if the machine is fresh (the settings file is gitignored, so a new checkout has none while the DECISION is already recorded), and re-probe the channel for the current session — a channel that answered yesterday is not a fact about today.

**The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.5.1

One flag in the fan-out dispatch, wrong in two independent ways, plus the gitignored file that would have undone the fix.

### Fixed

- **Fan-out workers run under `--permission-mode auto`, never `bypassPermissions` (UNBREAKABLE).** The flag was chosen on the premise that it is what lets an unattended worker finish. **The premise is false and it was measured: `bypassPermissions` asks for confirmation EVERY time** — entering it is itself a gated act, so rather than removing prompts it guarantees one, and the worker sits waiting for an answer nobody will give. It breaks exactly the automation it was picked to enable, and it fails in the direction that looks like a hang rather than an error, which is the expensive direction. The safety argument points the same way and would have sufficed alone: `bypassPermissions` evaluates no permission rules at all, `deny` included, so v5.5.0's session-start step wrote a block to keep `rm -rf /*`, `sudo *` and `curl * | sh` out of reach and the fan-out then handed that exemption to the only sessions that are simultaneously parallel, unattended and writing to real branches — the worst place in the skill to remove a barrier, and the one where nobody is watching. `auto` is not a drop-in and the cost is stated rather than left to be discovered: a worker in `-p` mode that reaches an action the allow-list does not cover cannot be asked, so the call is refused and the slice may fail. That is the correct failure direction — a refusal surfaces in the worker's report and is fixed by extending the allow-list, whereas an unattended `sudo` is fixed by nothing. **If workers stall, extend the allow-list; never go back to bypassing.**
- **The dispatch copies `.claude/settings.local.json` into each worktree before launching.** Found while verifying the fix above, and it silently undoes it: that file is gitignored — which is what makes it machine-local — so `git worktree add` produces a clean checkout WITHOUT it, and the worker falls back to user-level configuration with no project `deny` block, no project `env.PATH` and no project allow-list. The flag would be right and the directory wrong, which is worse than the original bug because it looks fixed. Its absence is invisible at runtime and shows up only as a worker mysteriously blocked, or mysteriously unblocked.

### Changed

- **The `env.PATH` warning gains the symptom that does not look like `PATH` at all: a re-authentication loop.** On macOS the keychain is reached through `/usr/bin/security`, which git and `gh` credential helpers invoke by name. Drop `/usr/bin` from `PATH` and that lookup fails, so the helper cannot read a credential sitting right there — and instead of an error naming a missing binary, the tool asks to authenticate, succeeds, fails to store, and asks again. The user debugs tokens, scopes, two-factor and the forge's settings, none of which is broken. The rule added: whenever authentication loops for no reason, check `PATH` for `/usr/bin` before touching a single credential, and never rotate a token on that evidence — it fixes nothing here and costs whatever else was using it.

**The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.5.2

The launch of the next chat, fixed at the four places a real close-out broke it — five windows opened, four killed by hand, and every one of the receipt's protections absent because no receipt was ever claimed.

### Fixed

- **A session NEVER composes the launch itself — it runs `scripts/keel-continue`, or it prints the prompt (UNBREAKABLE).** The courier checks have carried this rule since v5.3.0 ("A session never composes these checks itself"); the launch never did, so the skill said how many times it may be fired and never said what is allowed to fire it. A session that finds no `scripts/keel-continue` — an older project, a scaffold that predates it, a checkout where it was never generated — prints the prompt and stops. It does not reach for `osascript`, `open`, or a terminal of any kind. The absent script is not an obstacle to work around, it is the answer: no script means no chaining on this project yet, which is exactly the `off` behaviour the user already knows how to live with, and regenerating it is a maintenance touch on a later session rather than an improvisation at the end of this one.
- **The `start` action carries `cd '<absolute repo root>' &&`, and the registry's claim that it "inherits `cwd`" is gone.** That claim is true of a direct exec — `claude '<prompt>'` as a child of a session already standing in the repository — and **false of the action this skill actually verified for `start`**, because `osascript` asking Terminal.app to `do script` opens a NEW login shell, which starts in `$HOME` and inherits nothing from the process that asked. The two statements sat six lines apart in the same file and the false one was the reassuring one. Note what does NOT catch this: containment, `Repo:` and the absolute hand-off path are checks the ARRIVING session runs on a file it was told to read, so a session that lands in `$HOME`, reads no hand-off and finds no repository runs none of them — it simply starts inventing, with nobody watching. The `cd` is the only thing between `start` and a chat working in the wrong directory.
- **A launch that fired WRONG is spent, exactly like one that fired unobserved.** "A launch is NEVER retried" was written for "I cannot tell whether a window appeared", and in those words it does not cover "a window appeared and I can see it is wrong" — so the close-out killed the process it had just launched and fired again, four times, each iteration technically honouring the rule because each launch had in fact succeeded. Stated for both cases now: once the launch has fired, this session's chaining is over, whatever the outcome. A wrong window is reported in one line and the prompt is printed. Killing another session's process to make room for a better attempt remains forbidden for the reasons already recorded under "Designs measured and rejected" — no cleanup runs, the lane is never released, an orphaned lock is guaranteed — and it is forbidden even when the killer is the session that fired it.
- **`references/phase-5-development.md` §1 stopped contradicting `references/project-state.md` about what the scaffold generates.** It described `scripts/keel-continue` as "resolve the platform's open command … fire the constant URI once" — which is the `prefill` mechanism — for a script that must also serve `start`, the exact conflation `project-state.md` warns produces the headless variant: the next session runs correctly and no window ever appears. The scaffold line now names the nine-point contract, states that the two tiers do not share a command, and carries the `cd`.

### Changed

- **Quoting is recorded as the second reason the launch belongs in a generated script.** `osascript -e 'tell application "Terminal" to do script "…"'` nests three levels — the shell's quotes, AppleScript's, and the inner command's — around a payload containing absolute paths and a prompt sentence. Hand-composed at close-out time it is a coin flip: the measured close-out got it wrong twice before writing the command to a file and running that instead. A script written once, with the prompt passed through a file rather than interpolated into three quoting layers, has the problem zero times.

**Reconciliation:** regenerate `scripts/keel-continue` at the next maintenance touch on any project whose card is not `Chaining: off` — the `cd` is the change that matters, and a script generated before v5.5.2 does not have it. **The lock block's content did not change**, only its version stamp, so the refresh at the next freshness check is a stamp-only rewrite.

## 5.6.0

The work stops being able to exist in only one place. A project could be started, specified and half-built inside one folder on one computer — no repository, no remote, nothing replicated — and Keel, which has rules about accent marks and minified assets, said nothing about the disk that holds every artifact it produces.

### Added

- **"Work never lives only on this machine" — a new UNBREAKABLE cross-cutting section in `SKILL.md`, and Question 0 of the session-start setup batch, which is now four questions.** It is asked FIRST and answered BEFORE anything is created, because it decides whether the rest of the session's output exists in one place or two. Two things satisfy it and Keel checks both with commands rather than impressions: a Git repository with a remote (`git rev-parse --git-dir`, `git remote -v`), or a tree whose absolute path sits inside a location that replicates off the machine — Dropbox, iCloud Drive, Google Drive, OneDrive, pCloud, Sync, Nextcloud, a network volume — **confirmed with the user, never inferred from a folder's name**. What is missing comes back as a concrete offer, not a warning to nod at: `git init` and the first commit, publishing to the forge the user names (Keel prepares the exact commands and runs them where a forge CLI is authenticated), or moving the project inside a synced folder.
- **`Durability:` on the project card**, recording what covers the requirement — or `NONE — accepted risk (D-0XX)` where the user declined, which is their right and is recorded as an accepted risk with its consequence written out. **A repository with no remote is explicitly a partial answer, not a pass:** local commits protect against a bad edit and against nothing that happens to the disk.
- **Two `scripts/keel-verify` checks**, because a trap closed by prose alone is still open — this skill's own rule, applied to its own new rule. A remoteless repository that carries no recorded decision now **fails** instead of being skipped, which is the inversion that matters: the worst case had been the one case nothing reported. And a dirty working tree fails, naming the files — with the fix stated as the commit, never as a `.gitignore` entry chosen to make the check green.

### Changed

- **Nothing stays uncommitted (UNBREAKABLE), and it is stated where it was previously only implied.** Every working block — a slice, a test point, a sprint close, a session end, a stop of any kind — ends with the work committed to `develop` or to a work branch bound for it, and a repository carrying only `main`/`master` gets `develop` created first rather than product work landing on the release branch. Outside automatic mode the PUSH may wait for the user, as it always has; **the commit never does.** Withholding it leaves the work in the one place that survives nothing, which is the opposite of deferring safely.
- **Automatic mode is deliberately untouched.** The question is asked once and lives on the card; every later session re-runs two cheap commands and says one line if reality no longer matches — it never re-asks, so a removed remote surfaces the day it happens instead of the day it matters. And in automatic mode Keel performs `git init`, the first commit, the creation of `develop`, and every commit and push itself without asking. Only choosing WHERE the work is published — which forge, which account, which folder — stays the person's, because it is theirs to own and not because it needs a permission dialog.
- **Adoption checks it too, and that is where the exposure is largest**: an adopted repository carries years of work, so "it exists only on this laptop" is a far bigger number than on a project born five minutes ago. Adoption also inventories what arrives already uncommitted in the working tree and commits it, after the confidential-data check, rather than leaving it stranded.

**Reconciliation:** answer Question 0 once on every existing project and add the `Durability:` card line, verifying it with commands rather than memory — a repo whose remote was removed is exactly the case this finds. Add the two checks to `scripts/keel-verify` at the next touch on projects that have one. **The lock block changed** — item 4 gains the never-leave-work-uncommitted duty and the no-remote warning — so `CLAUDE.md` and `AGENTS.md` (and any `GEMINI.md` mirror) need a real content refresh at the next lock-freshness check, not a stamp-only rewrite.

## 5.7.0

Closing the laptop at a sprint close stops costing anything. The hand-off was already specified, already written to a fixed path, already verified by a script — and it still failed the one case the user actually lives in: finishing a sprint, walking away, and opening a fresh chat days later with a one-word prompt.

### Changed

- **The continuation file is written at EVERY sprint close, unconditionally (UNBREAKABLE).** The duty existed (Phase 5 §5, step 11) but every surface around it framed the file as a SESSION-END artifact — "whenever the session is ending", "any session ending mid-work produced a continuation prompt", and item 5 of the portability lock in the same words. In automatic mode a session normally carries straight on into the next sprint, so the close was precisely the case the wording did not compel: the moment a person actually gets up from the desk. It now holds in automatic mode exactly as in manual, and on `Chaining: off` exactly as on `start` — **chaining decides whether a next chat is OPENED, never whether the hand-off EXISTS.** Stated in the four places a session reads: `SKILL.md`, `references/project-state.md` (the file's own section, the template intro, the definition of done), `references/phase-5-development.md` (step 11 and the phase's definition of done), and the lock.
- **A hand-off that is not current is worse than no hand-off (UNBREAKABLE)** — the half that was missing entirely, and the half that would have broken the guarantee even with the file written. The courier checks compare `Commit` and `Tree` against the repository, so a file written at a close and then left behind by three more slices comes back `VERDICT: STOP` and the fresh chat refuses to continue: it looks exactly like insurance right up to the moment it is needed. A session that carries on past a close therefore **regenerates it as the work advances** — at every commit point, which in Phase 5 means every test point that commits — and unconditionally before ending. One small overwrite per test point, against the reconstruction the whole mechanism exists to eliminate.
- **Reading the hand-off is the FIRST thing a resuming session does — before the state files.** This is what makes a bare "continue" enough to restart the work, with no recap, no pasted wall of text and no memory of the last session. The fixed session-start order began at `docs/PROGRESS.md`, so the file was only ever consumed when someone pointed a session at it — which works for a chained chat and is useless for a chat opened by hand days later. Now in the Resume entry mode (`SKILL.md`), in the fixed reading order (`references/project-state.md`), and in **item 2 of the portability lock**, so it binds an assistant that opens the repository without the skill installed. A missing file stays ordinary: it is gitignored, so a fresh clone has none, and the normal order simply proceeds.

### Fixed

- **`VERDICT: STOP` costs the FILE its authority, not the SESSION its work.** The courier table has always ended "STOP and say exactly what disagrees, then resume from `docs/PROGRESS.md` instead" — but the phrasing carried in `SKILL.md`, "stops on a `VERDICT: STOP`", says the opposite when read alone, and it is the sentence a session is most likely to meet first. A stale or foreign hand-off is discarded, the session says so in one line, and it resumes from the committed state. Being paralysed by a courier one was free to ignore is not a safety property.
- **The hand-off is a courier and never an authority**, now written wherever it is read: where it and the committed state disagree, `docs/PROGRESS.md` wins and the divergence is stated.

**Reconciliation:** nothing to migrate and no new artifact — this is behaviour, applicable at the next sprint close and the next session start. From the next close on any project, write `docs/continuation-prompt.md` whatever the `Autonomy:` and `Chaining:` lines say, and regenerate it as the work advances rather than leaving it describing a commit that is no longer `HEAD`. **The lock block changed in substance for the second release running** — item 2 gains the read-the-hand-off-first duty and item 5 the sprint-close duty — so `CLAUDE.md` and `AGENTS.md` (and any `GEMINI.md` mirror) need a real content refresh at the next lock-freshness check, not a stamp-only rewrite.

## 5.8.0

Forge issues stop waiting for the next sprint to END. The after-sprint duty guarded one edge of the sprint and left the other open: between two sprints there can be days, and everything that arrived in them — a fresh bug report, or the reply on an `awaiting reporter` issue that decides whether it closes or reopens — sat unread while the new sprint was planned as if none of it existed.

### Added

- **A sprint kickoff sweeps the forge before it plans anything** (Phase 5, "Sprint kickoff", now step 1 — ahead of plan mode and the re-validation). It runs the same inbound sweep the close runs: new issues triaged into `docs/issues.md`, and new comments on existing issues read BY STATUS — on `awaiting reporter` the comment is the verdict (confirms → closed, recorded under `Closed by:`; "still broken" → the work reopens with that report as new evidence), and anything only the user can answer is a stop with its notification, never a guess published on a public thread. **A fresh issue the sweep turns up belongs in the same conversation as what this sprint is about to contain**, not in a note for later — that is the whole point of sweeping before the plan instead of after it.
- **`Last inbound sweep:` in `docs/issues.md`** — one header line, stamped the moment ANY inbound sweep finishes (sprint close or maintenance touch), whether or not it found anything. It is what makes the kickoff check mechanical rather than a recollection of whether someone looked.
- **`Issue sweep interval:` on the project card's `Autonomy:` line** — default 24h, asked once with the after-sprint duty in the session-start setup batch, `n/a` on any project that did not accept the duty. The kickoff compares it against `Last inbound sweep:`: older or missing → sweep now; within the window → say so in one line ("issues last swept Xh ago, within window") and move on. **A sprint chained seconds after a close therefore pays nothing** — the close just wrote the timestamp — which is the only difference between a genuine kickoff and a continuation, and it is decided by data rather than by the session's memory of itself.

### Changed

- The definition of done in `references/project-state.md` now fails a project whose `Last inbound sweep:` is older than its interval while a sprint is open — the same shape as every other state check: verifiable from the files, not from what anyone remembers doing.
- The setup batch's description in `references/phase-1-discovery.md` and `references/adoption.md` names the interval as part of the issue question, so it is asked once with the duty instead of being invented later by whichever session first needs it.

### Fixed

- **`MANIFEST.md` structure**: v5.7.0's Table 3 row had been written into the middle of Table 2, and a stray blank line split Table 3 in two since v5.4.1 — both tables now parse as one table each. Content unchanged in both cases; the manifest is read mechanically after every update, so a broken table is a real defect.

**Reconciliation:** nothing to migrate, no new file, and **the lock block is UNCHANGED** — after two releases that needed a real content refresh, this one is a stamp-only rewrite at the next lock-freshness check. On any project carrying the after-sprint duty: add `Issue sweep interval:` to the card's `Autonomy:` line (24h unless the user says otherwise), add the `Last inbound sweep:` line to `docs/issues.md` (`never` until the first sweep stamps it), and from the next sprint kickoff on, sweep before planning whenever that line is missing or stale. Projects on `issues: on-request` or with no forge are unaffected beyond the `n/a` card value.

## 5.8.1

The chained chat stops deadlocking on the chat that launched it. Under `Chaining: start` the whole mechanism worked exactly as written and produced nothing: the closing session fired the launch while still holding the single lane, the window it opened arrived two seconds later, found the lane held by a PID that was genuinely alive, and stopped. Measured on a real project — not a fork, a deadlock, at link one, with the previous chat's window sitting empty on screen holding a lane it had finished with. No guard misbehaved. The moment named for the release was simply one that never arrives.

### Fixed

- **The lane is released at the CLOSE-OUT, not "on exit" (UNBREAKABLE).** The old rule — "the holder releases it on exit, every exit, including the failing ones" — is unimplementable by the party it names: the holder is a SESSION, and a session does not get to run code when it ends. Its process outlives it; a CLI chat stays alive and idle in its window for as long as the window is open, and under `start` nobody is there to close the window. The release therefore moves to a moment the session can actually act on: the close-out's last act, `scripts/keel-handoff-verify --release`, which frees the lane only if this session is its recorded owner. This is the whole defect: a duty written against the wrong actor reads as satisfied and is never performed.
- **Every close-out releases, not only a chaining one.** `off` and `prefill` are not exempt, and exempting them would be the same error one level down — the user who opens a chat by hand ten seconds later hits precisely the wall `start` hit. The step runs on every path, blocked hand-off included, and is silent and safe on any project that never took a lane.
- **`scripts/keel-continue` hands the lane back IMMEDIATELY BEFORE firing** (contract point 7). The ordering is not a detail: the launched window checks the lane within seconds of opening, so a release that lands afterwards is a race the chain loses — and it loses it looking exactly like the lane working correctly. The script still never TAKES the lane; it gives back what its own session already held, which is the only reading of "the launcher never touches the lock" that leaves the chain able to move.
- **The baton, because a release that depends on the previous session behaving is not a mechanism.** `keel-continue` now writes its own session PID into the launch receipt at the moment it claims it, and the arriving session — verifying the same hand-off, therefore deriving the same receipt identity — compares the live lane holder against it. Holder equals this hand-off's recorded launcher → that holder is by definition a session that already closed out, and the lane passes as a baton rather than stopping the chain. It covers the cases the close-out release cannot: a session interrupted after firing, one that crashed, one still running a `keel-continue` generated before this version. Any other live holder still stops the session, and the check is never loosened into "the holder looks idle" — that would be a guess about another session, where this is the launcher's own statement about itself.
- **A busy-lane `STOP` now names the remedy.** The holder's PID, its start time, and the one thing that frees it: closing that window, or running `scripts/keel-handoff-verify --release` in it. The user is the only party who can clear a lane held by a live session, and the message that told them to stop was giving them a deadlock with no vocabulary for it.

**Reconciliation:** no new file, no card line, and **the lock block is UNCHANGED** — a stamp-only rewrite at the next lock-freshness check. On any project whose card is not `Chaining: off`, the next maintenance touch regenerates both scripts (`keel-continue`: record the PID in the receipt, release before firing; `keel-handoff-verify`: `--release`, the baton, the fuller STOP message) and the close-out gains its final release step. A lane already stuck this way on a project not yet regenerated is cleared by closing the previous chat's window, or by running `scripts/keel-handoff-verify --release` in it.

## 5.9.0

The autonomous cycle stops being interrupted — twice over, by two unrelated mechanisms that produced the same symptom. Once from outside: `auto` mode removes the static-matching problem but routes every command through a classifier, and that classifier reads a comment on a client's issue as an outward-facing action to confirm. Correct default; wrong for this cycle, where replying to the reporter and asking them questions IS the work. Once from inside: a stop written at session scope in a table whose every row describes a single piece of work. Measured on one real project, in one session: the sweep found the issue, built the fix, opened the PR, then stopped to ask permission to tell the reporter about it — and later, with two issues awaiting answers, a merge only the user could perform, and a third issue sitting there fully buildable, reported the blocks correctly, recorded everything correctly, and handed back the afternoon.

### Fixed — the interruption from outside

- **The recommended `.claude/settings.local.json` gains an `allow` block for the forge's conversational surface** (`references/keel-maintenance.md`): reading, commenting, asking, labelling, opening, closing and reopening issues, and merging a PR. Setting `defaultMode: "auto"` was never sufficient on its own and the file said nothing about it, so every project written from this guidance inherited the same interruption. The merge instruction now unions `allow` too, so an existing file catches up without losing a rule the user put there.
- **What is left OUT of that block is part of the fix.** No `gh repo delete`, no `gh release create`, no blanket `gh api`. Publishing a release is a Phase 7 decision the user approves by name; it stays a dialog, and a permission block that quietly swallowed it would be trading the wrong thing for the automatism.
- **The `develop` → `main` boundary is now documented as a rule that needs a mechanism.** `Bash(gh pr merge:*)` is a prefix rule and cannot tell a PR based on `develop` from one based on `main` — the two commands are the same text, and no `ask` or `deny` entry can separate them either, because the target branch is not in the command line. The note that used to end at "which Keel never performs on its own initiative" now says what enforcing it actually takes: a `PreToolUse` hook that resolves the real target (current branch, following any `checkout`/`switch` earlier in the same command line, plus the PR's `baseRefName`) and fails closed when it cannot tell. Keel does not write that hook — it is the user's own machine configuration — but "the assistant simply does not do it" was being offered as a guarantee, and it is not one.

### Fixed — the interruption from inside

Two rules were saying different things and the wrong one was winning, for a structural reason: "Finish the queue; stop only on a real dependency" is written for the moment a block of work ENDS, while "Stopping means: … then wait" is what the assistant meets the instant something blocks MID-FLIGHT. The second is the one in hand at the decisive moment.

- **A stop is scoped to the ITEM, never to the SESSION (UNBREAKABLE)** (`SKILL.md`, "When to stop and ask"). Every row of that table says one piece of work cannot proceed on the assistant's own judgment; none says there is nothing else to do. So the stop parks that item — question asked, notification sent, state recorded — and the session moves to the next thing in the queue that does not depend on the answer. Waiting is correct only when nothing independent remains, and that is established by looking, not assumed because something is blocked. Several parked items are listed together so the user answers them in one pass instead of being made the bottleneck of each in turn.
- **"Finish the queue" now says it holds mid-block, not only at the end of one.** The rule listed the two reasons Keel stops; it now says that *every* remaining step must depend on the missing thing, and names the reading that was being lost — "the thing I was doing got blocked" is a reason to switch items, never a reason to stop.
- **`Handover: blocked` describes the SESSION, not an item in it** (`references/project-state.md`). Chaining fired only on a clean hand-off, and any "When to stop and ask" row wrote `blocked` — so one parked question cost the entire next session: nothing was opened, and the user returned to a chain stopped at a link that had somewhere to go. A hand-off with parked items and buildable work left is `clean` and CARRIES the parked list as open items, so nothing is lost by not stopping. Genuinely blocked still means blocked, and still never chains.

### Fixed — the language of the forge

The language contract covered two cases, "everything Keel creates" and "the conversation with the user", and a forge issue is neither. Read as the first — which is the reading the wording invites — it answers a Spanish bug report in English.

- **An issue Keel OPENS is always English**, title and body, whatever language the conversation runs in: a public, permanent, indexed artifact of the project, read by contributors who were never in the conversation.
- **A reply to an issue somebody else opened follows THAT issue's language**, read from the reporter's own text — never from the repository, never from the conversation, never guessed from a name, a locale or a timezone. Mixed thread → the language the reporter last used; genuinely undecidable → English. This is the half the English default gets wrong, and the cost of getting it wrong lands on the person who did the project the favour of reporting the bug, on their own thread. Recorded in both places the messages are actually written: `SKILL.md` "Token economy" and the three-beat lifecycle in `references/project-state.md`. The orthography contract holds in whatever language it lands in — a reply in Spanish carries every accent and every ñ.

### Added — issue capture, asked and never assumed

A defect the user reports in conversation is worked and then disappears: what was wrong, what it turned out to be and how it was fixed live in a chat log and nowhere a person can search in six months.

- **New setup question (2b) and new card value `Issue capture: on|off|n/a no forge`.** With `on`, a problem the user reports becomes a forge issue, and the issue is **opened BEFORE the work starts** — that ordering is the point, because a session that dies mid-fix, runs out of context or is interrupted then leaves the report standing on the forge rather than only in the chat. Keel comments as the work advances (what it turned out to be, the commit or PR, anything that changed the diagnosis), not only at the end.
- **It is ASKED, never assumed.** A tracker is a shared public surface, and a project that keeps its tracker for third-party reports is entitled to that; filling it with entries nobody asked for is not a favour. The question is only put where the project has a forge.
- **A duplicate is a comment, not a second issue** — if an open issue already covers it, the information goes there. And the user still closes it: "Keel never closes an issue on its own reading of the code" holds unchanged, and the reporter being the person in the chat does not soften it.

**Reconciliation:** nothing to migrate, no new file, and **the lock block is UNCHANGED** — a stamp-only rewrite at the next lock-freshness check. **One new card value:** ask Question 2b once on every existing project that has a forge and record `Issue capture:` on the `Autonomy:` line — a project with no forge records `n/a no forge` and nothing changes for it. Then, where it applies: on any project whose `.claude/settings.local.json` Keel wrote, the next maintenance touch merges the new `allow` block into it (union, never overwrite, never dropping a rule the user put there) and says so in one line. Projects with no forge, or answering `no` to the autonomy question, are unaffected — no file was written for them and none is now. The stop-scope rule needs no action at all: it lives in `SKILL.md`, which the lock's item 1 already requires reading in full every session, so every project picks it up the moment its copy is current, from the next session on.

